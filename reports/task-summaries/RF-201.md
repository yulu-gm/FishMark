# RF-201 Persistent Text Buffer and Session Revisions

Task: `RF-201`

结果：`PASS` — independent specification/architecture review and formal task acceptance both passed on 2026-08-04.

## 完成内容

- Added a runtime-neutral domain `TextBuffer` and explicit `TextBufferFactory`; production main injects the private CodeMirror `Text` implementation from `@fishmark/workspace-infrastructure`.
- Added immutable revisioned document edit batches with atomic original-offset validation, typed applied/duplicate/conflict/sequence-gap/invalid outcomes, and exactly one revision increment for each accepted non-empty batch, including same-text replacements.
- Added contiguous per-client acknowledged sequence high-watermarks so retries are idempotent without storing one record per edit.
- Preserved Unicode/UTF-16 offsets, emoji, CRLF, multi-change, large-document/insertion, invalid-range, stale-revision, sequence-gap, and duplicate semantics.
- Activated one-public-entry, runtime-resolution, dependency-allowlist, and source-root-containment guards for workspace infrastructure.
- Preserved the existing renderer/preload full-draft contract as a temporary transport path owned for deletion by RF-204; it is not a second canonical document owner.
- Closed the review findings for runtime-malformed inserts and relative-import source-root escapes with focused RED-to-GREEN evidence.

## 验收依据

- Production sessions use CodeMirror `Text` behind the domain port; domain/application public types remain free of CodeMirror types.
- Main explicitly injects the infrastructure factory; there is no production fallback, global mutable factory, compatibility facade, or parallel canonical session.
- Infrastructure applies persistent replacements without rebuilding the complete document string on the incremental path.
- Independent specification/architecture review: final `PASS`, P0/P1/P2 = 0, no open questions. Its initial P1 progress-document inconsistency was corrected and re-reviewed to `PASS`.
- Final code-quality review: Critical/Important/Minor = 0, `Ready: Yes`. The initially reported Important malformed-insert gap and Minor relative-import escape were repaired and re-verified RED to GREEN.

## 验证

- Post-fix domain/infrastructure focus: 4 files / 163 tests passed.
- Post-fix architecture focus: 1 file / 209 tests passed.
- Earlier fresh cross-layer focus: 63 files / 816 passed plus 1 existing skip.
- `npm.cmd run test:editor-foundation`: 7 files / 285 tests passed.
- `npm.cmd run lint`: passed with 0 errors / 8 existing warnings.
- `npm.cmd run typecheck`: passed.
- `npm.cmd test -- --reporter=dot --silent=passed-only`: 166 files; 2,162 passed plus 1 explicit skip out of 2,163 total; exit 0.
- `npm.cmd run build`: passed for renderer, Electron, CLI, and all three workspace runtime verifiers; the existing `>500 kB` chunk advisory remains.
- Formal `npm.cmd run test:editor-behavior`: 121/121 cases and 2,541/2,541 targets; 79 existing, 1,928 runner, 534 known defects, 0 unexpected, 0 not-run; 24,614 ms; exit 0.
- Final acceptance-document `git diff --check`: exit 0; only line-ending conversion warnings were reported.

## 人工验收

1. Launch the built editor, create an untitled document, and enter Unicode, emoji, combining marks, and mixed LF/CRLF content; save, close, and reopen it to confirm exact Markdown round-trip behavior.
2. Open a file-backed tab and exercise edit, Save, Save As, reload, reorder, move/detach, and close; confirm existing full-draft user behavior and RF-101/RF-102 ownership/lease behavior remain unchanged.
3. Retry the same edit-batch sequence through a focused integration or diagnostic harness; confirm the edit is not reapplied and the revision does not advance again.
4. Submit stale-revision, sequence-gap, overlapping/out-of-range, unsafe-integer, empty, and runtime-malformed changes; confirm distinct typed rejection results and unchanged revision/high-watermark state.
5. Inspect the emitted Electron build and run the workspace runtime verifiers; confirm infrastructure resolves only through its public entry and production main composes the CodeMirror-backed factory.

## 剩余风险或未覆盖项

- RF-202 owns the shared/preload revisioned edit DTO and sender-validated main handler; it is dependency-ready but remains `PLANNED` and has not started.
- RF-203 owns renderer client identities, pending queues, conflict/rebase behavior, and client lifecycle/ledger retirement.
- RF-204 owns deletion of full-draft synchronization. The current full-draft path intentionally remains as transport compatibility, not a second document owner.
- RF-201 claims no timing budget. Measured persistent-buffer performance and final budgets remain RF-901 work.
- Lint retains 8 existing warnings, build retains the existing large-chunk advisory, and the formal behavior baseline retains 534 exact known defects; RF-201 introduced 0 unexpected and 0 not-run outcomes.

## 状态

- RF-201: `COMPLETE`
- M2: 1 / 4, `IN_PROGRESS`
- Program: 5 / 38 tasks complete
- Current task: none
- Next dependency-ready task: RF-202 (`PLANNED`, not started)
