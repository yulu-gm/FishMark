# RF-702 — HTML Export canonical cutover

Status: **COMPLETE** (2026-09-21)

## Outcome

- Renderer export orchestration owns only canonical tree creation, shared render-plan construction, theme/style collection, and the outer HTML document shell.
- Pure Markdown-content HTML rendering lives in `@fishmark/markdown-presentation`.
- Export no longer calls `parseMarkdownDocument`, `parseInlineAst`, or `collectReferenceDefinitions` to reinterpret source.
- Inline/table/reference/footnote data comes from the canonical tree. KaTeX remains behind the existing lazy renderer export chunk and is injected into presentation through `renderMath`, so editor consumers of the presentation package do not pull KaTeX into the initial graph.

## Acceptance evidence

Owner-provided clean-tree verification at `1b08a2c`:

- focused 5 files / 42 tests: PASS
- typecheck: exit 0
- build: exit 0
- full Vitest: 2799 passed / 1 skipped / 11 known failed, no new failure
- architecture guard: 234/234
- bundle source-group/lazy contracts: KaTeX and Mermaid forbidden-initial PASS; all four required lazy chunks PASS
- `perf:bundle` remains red only for four pre-existing maximum budgets, owned by RF-506/M5 and M9 final performance work.

RF-702 therefore satisfies its scoped exit condition without weakening the global bundle contract.
