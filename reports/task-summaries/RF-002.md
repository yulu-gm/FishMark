# RF-002 Architecture and Performance Guards

Task: `RF-002`

结果：`PASS` — independent architecture acceptance and formal task acceptance both passed on 2026-07-15.

## Completed

- Established `fixtures/architecture/editor-foundation-guard.json` as the only versioned package/import/parser-lifecycle and public bundle-policy source, with fail-closed validation and 73 exact RF-604-owned CodeMirror exceptions.
- Enforced active main/preload/renderer/markdown-engine/editor-core boundaries, public package entries, unique active package-rule ownership, case-folded resolved repository identities, stale debt rejection, and planned-package activation.
- Registered current public/internal Markdown parse entries and direct micromark document sites through RF-405, including the supported static, dynamic, import-equals, literal-require, alias, computed-property, and transparent TypeScript wrapper forms.
- Committed the only RF long-document fixture at exactly 20,000 LF-only lines with SHA-256 `545601ad9d770898e23e5551e938bcaa4ebaecd8446941a4145a4db8777f6d37`.
- Instrumented actual micromark `.document()` boundaries without a global mutable hook. The stable full-parse counts are open 504, edit 502, selection 0, ordered-list edit 503, outline 502, and metrics 503.
- Recorded honest zero incremental-window/cache-hit/invalidated-node counters with `incremental-structure-cache-not-implemented`; durations remain informational rather than cross-machine CI limits.
- Bound emitted renderer chunks to schema-v1 Vite provenance, final code/map hashes, `moduleIds`, import graphs, and explicit Source Map state. Attested mapless chunks use `NOT_EMITTED`; no identity-map or compatibility fallback remains.
- Deleted subordinate performance formatters and the virtual identity-map helper; architecture review found no remaining compatibility or dead path in RF-002 scope.
- Final architecture closure commits are `3e0c34e`, `3d8c2b4`, and `53824a7`.
- Changed no user-visible editor behavior, stored Markdown, parser result, selection mapping, undo grouping, IME routing, workspace ownership, or persistence flow.

## Acceptance Criteria

All 13 RF-002 intake criteria are `PASS`:

1. The versioned, non-empty manifest is the only validated rule source.
2. All current process/package boundaries are actively constrained; absent planned packages are not reported as passes.
3. Every temporary exception is exact, justified, owned, and stale-checked.
4. Current parser entries remain available under registered roles and retirement lifecycle; unauthorized public parsers or micromark document sites fail.
5. Synthetic forbidden imports, package-internal imports, parser entries, invalid rules, and retired symbols/paths fail deterministically.
6. The committed 20,000-line fixture matches its declared bytes, line policy, source length, and SHA-256 before measurement.
7. The RF baseline reads the committed file directly; the legacy generator is not a second canonical 20k source.
8. Open, edit, selection, ordered-list edit, outline, and metrics expose the five required counters and parser-entry detail.
9. Unavailable incremental behavior is reported as zero with a stable capability reason and no invented reuse claim.
10. Durations are finite/non-negative information; deterministic identity/schema/counter/import/bundle invariants are the gates.
11. Bundle JSON and provenance are versioned, deterministic, hash-bound, module-graph authoritative, and explicit about emitted versus `NOT_EMITTED` maps.
12. Formal behavior evidence confirms no product editing regression.
13. Focused verification, full mixed-code gates, independent architecture acceptance, and formal task acceptance all pass with fresh evidence.

`TC-060A` is fully covered by the public foundation/performance gates and their synthetic fail-closed unit matrix.

## Fresh Validation

- `npm.cmd run test:editor-foundation`: `PASS`; 7 files, 253 tests.
- `npm.cmd run perf:baseline`: `PASS`; contract SHA-256 `711b83a6270ae9fcaad6c8d52d4d02efdbc2a98f16d8fd45be12ebbe945f02af`, all 23 manifest-owned bundle checks passed, the canonical fixture/counter report passed, and the final foundation run completed 7 files / 253 tests.
- `npm.cmd run lint`: `PASS`; 0 errors and 8 pre-existing unused-disable warnings (7 in `src/renderer/editor/App.tsx`, 1 in `src/renderer/editor/settings-view.tsx`).
- `npm.cmd run typecheck`: `PASS` across renderer, Electron, Vitest, and CLI TypeScript projects.
- `npm.cmd test`: `PASS`; 138 files, 1,738 tests.
- `npm.cmd run build`: `PASS`; renderer, Electron, and CLI built. The existing Vite chunk larger-than-500-kB warning remains informational.
- `npm.cmd run test:editor-behavior`: `PASS` on the first exclusive acceptance attempt; 121/121 cases, 2,541/2,541 targets, 79 verified-existing, 1,928 verified-runner, 534 known-defect-observed, 0 unexpected, 0 not-run, 24.570s.
- Ordinary build artifact check: `PASS`; `dist` contains 0 `.js.map` files and no `fishmark-bundle-provenance.json`.
- `git diff --check`: `PASS`.
- Independent `fishmark-architecture-acceptance` over `a6da237..53824a7`: `PASS`; 0 blocking findings; open questions: none.

## Manual Acceptance

1. Run `npm.cmd run perf:baseline`; confirm contract SHA-256 `711b83a6270ae9fcaad6c8d52d4d02efdbc2a98f16d8fd45be12ebbe945f02af`, `bundleBudget=PASS`, and exactly 23 check rows with `status=PASS`.
2. Inspect the performance report and confirm fixture SHA-256 `545601ad9d770898e23e5551e938bcaa4ebaecd8446941a4145a4db8777f6d37`, 20,000 lines, the six full-parse counts above, selection-only zero parse/rebuild, and the stable unavailable-capability reason.
3. Inspect analyzer JSON from a sourcemap build; confirm `moduleIds` provenance is forbidden-source-group authority, emitted maps are `COMPLETE`, and Vite-attested mapless chunks are `NOT_EMITTED` with `map: null`.
4. Run the analyzer/provenance unit tests and confirm missing/tampered provenance, code/map/payload/import mismatches, malformed Source Maps, and inconsistent nullable map fields fail non-zero.
5. Run ordinary `npm.cmd run build`; confirm no `.js.map` or `fishmark-bundle-provenance.json` remains in `dist`.
6. In synthetic architecture fixtures, add a forbidden dependency, widened/stale exception, duplicate/case-aliased package rule, unregistered parser export, and wrapped direct micromark document parse; confirm each fails while documented negative forms remain negative.
7. Run the formal behavior gate exclusively and confirm 121/121 cases, 2,541/2,541 targets, zero unexpected, and zero not-run without changing the RF-001 calibration.

## Remaining Risks or Uncovered Work

- The current 20k fixture exposes repeated full scans and no incremental reuse; RF-404 owns the incremental structure cache and RF-405 owns parser hard cutover.
- The 73 exact CodeMirror dependency exceptions remain intentional RF-604 deletion debt.
- Duration samples are machine-dependent information; RF-901 owns percentile sampling and final latency budgets.
- The behavior baseline still contains 534 known product defects assigned to later roadmap tasks; RF-002 neither fixes nor adopts them as desired behavior.
- Two execution-phase full behavior attempts previously observed focus-derived visibility-only flakes. Isolated and later full runs, including the first exclusive formal acceptance run, were clean. Runner settlement/diagnostics remain separately scoped and must not alter calibration or editor semantics.
- Live macOS performance, IME, cursor, and geometry acceptance remains scheduled by later roadmap gates.

## Record Scope

The editor-foundation program intentionally uses `docs/refactor/editor-foundation/roadmap.md`, `docs/refactor/editor-foundation/progress.md`, and this summary as its backlog/progress/summary consistency set. `MVP_BACKLOG.md` and general `docs/progress.md` describe the separate MVP task ledger and were not modified. The acceptance commit contains documentation/status records only; the complete RF-002 implementation range is `a6da237..53824a7`, and no `dist` or `.artifacts` output is included.
