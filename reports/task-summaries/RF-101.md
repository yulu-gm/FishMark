# RF-101 Extract Workspace Domain

Task: `RF-101`

结果：`PASS` — independent architecture acceptance and formal task acceptance both passed on 2026-07-28.

## Completed

- Added the production, runtime-neutral `@fishmark/workspace-domain` package as the single owner of workspace, tab, document-session, immutable text-buffer, revision, saved-checkpoint, ordering, move, detach, and close rules.
- Replaced the main-local workspace service with one live main-owned `WorkspaceState` and one explicit immutable domain-to-IPC projection boundary.
- Established separate canonical location and filesystem-object identities, fail-closed ambiguous ownership, fixed `tab -> location -> object` locking, and captured owner/revision/checkpoint validation for open, Save, Save As, reload, close, reorder, move, and detach.
- Serialized same-tab document IO, reorder, and ownership transfer through one transaction owner while preserving concurrency across unrelated tabs.
- Made native window close an exact request-generation transaction with one confirmation scope, a held multi-tab lease through unregister, bounded transport, and a separate post-confirm watchdog.
- Rebuilt renderer workspace mutation ownership around one non-React `WorkspaceRendererApplication`, strict FIFO dispatch, per-tab exact-ack draft outbox, canonical known/unknown admission, and exact `{ tabId, epoch, loadRevision }` editor ownership.
- Added acknowledged `sealing -> sealed -> releasing` editor transitions, structural CodeMirror read-only enforcement, last-input capture before destructive IPC, fresh-state canonical undo boundaries, recoverable history preservation, and lifecycle fences against late/disposed work.
- Hard-cut raw test snapshot/mutation ownership, hook-owned queues, split save orchestration, shell-state draft reconciliation, rollback activation, direct transfer bypasses, parameterless native-close compatibility, renderer-selected watcher identity, and the old workspace service/types/exports.
- Preserved Markdown text as the only document truth and retained exact source text without whole-document normalization.

## Acceptance Criteria

All RF-101 roadmap steps and exit conditions are `PASS`:

1. Workspace projections are immutable and internal mutable session ownership is isolated in the pure domain package.
2. Dirty state derives from revision versus saved revision and exact saved text; restoring durable content becomes clean without another write.
3. Create/open/activate/close/reorder/move/detach rules have one canonical implementation and one main-owned live state.
4. Draft mutation and reorder are sender-derived owner compare-and-set operations; stale renderers receive no foreign projection.
5. Save, Save As, reload, close, reorder, move, and detach share the required transaction order and fail closed on stale identity, owner, revision, or lifecycle completion.
6. Reload exposes one typed success/revision-stale/error contract and preserves newer drafts plus conflict protection on stale or ambiguous completion.
7. Native close holds exact ordered ownership through unregister and has bounded transport/post-confirm liveness without timing out active native UI.
8. Detach readiness is single-flight, readiness is outside the tab lease, transfer commits the latest canonical session, and every failed target lifecycle leaves no ghost binding.
9. Renderer mutations have one application owner, one FIFO, exact draft acknowledgement, acknowledged editor transitions, and no React/test compatibility owner.
10. The old service, compatibility structures, dead exports, consumer deep imports, direct transfer bypasses, legacy draft/reload/close forms, and renderer-selected watch identity are absent.
11. Independent architecture acceptance over `f5aa70b..003d4d5` is `PASS` with P0/P1/P2 all zero and no open questions.
12. Focused, architecture/performance, full mixed-code, build, static, and formal editor-behavior gates pass with fresh acceptance evidence.

Relevant product baselines in `docs/acceptance.md` and `docs/test-cases.md` remain protected: local Markdown round-trip safety, save/close correctness, multi-window ownership, Undo/history behavior, editor responsiveness, and `TC-060A` architecture/performance evidence.

## Fresh Validation

- RF-101 renderer/editing focus: `PASS`; 13 files / 863 tests. It covers every test file changed by the final renderer closure plus editor source layout, block decorations, recursive block parsing, and list edits.
- Roadmap workspace-domain/main focus: `PASS`; 16 files / 217 tests.
- `npm.cmd run test:editor-foundation`: `PASS`; 7 files / 260 tests.
- `npm.cmd run typecheck`: `PASS` across workspace-domain declarations and renderer, Electron, Vitest, and CLI projects.
- `npm.cmd run lint`: `PASS`; 0 errors and 8 pre-existing unused-disable warnings.
- `npm.cmd run build`: `PASS`; renderer, workspace-domain, Electron, emitted runtime verifier, and CLI built. The existing Vite chunk-size warning remains informational.
- Escalated `npm.cmd test -- --reporter=dot --silent=passed-only`: `PASS`; 163 files / 2,052 passed plus 1 explicit Windows symlink capability skip out of 2,053 total.
- Escalated `npm.cmd run test:editor-behavior`: final exclusive run `PASS`; 121/121 cases, 2,541/2,541 targets, 79 verified-existing, 1,928 verified-runner, 534 known-defect-observed, 0 unexpected, 0 not-run, 24.565s. The immediately preceding attempt had 8 active-line `physical-geometry.visibility` timing mismatches only; source text and semantic geometry remained intact, and the full clean repeat used the same manifest/contract without calibration or runtime changes.
- Handoff zero-residue scans: `PASS`; obsolete service/state/deep-import/native-close/direct-transfer/draft/reload/shell-compatibility searches returned no production residue. The broader legacy-helper scan matched only negative source assertions; `input.currentPath` / `input.targetPath` matched only the documented unrelated open/export/save adapters and negative tests.
- `git diff --check`: `PASS` after acceptance record updates.

## Manual Acceptance

1. Create an untitled document, type Markdown, Save, and confirm the exact source is preserved and the tab becomes clean.
2. Edit while Save is in flight; confirm the later edit remains visible and dirty after the earlier write completes. Reverse Save/reload order and confirm memory and disk remain consistent.
3. Start reload, type a newer draft before disk completion, and confirm the draft/conflict warning remains and autosave stays protected. Retry without a concurrent edit and confirm the disk snapshot applies and clears conflict state.
4. Close a dirty tab with queued autosave using discard, save, cancel, and failed-write paths; confirm only a valid clean/durable state closes and no discarded draft is written.
5. Start native window close with multiple dirty tabs; exercise save/discard/cancel and a missing COMPLETE. Confirm exact ordered confirmation, no premature unregister, and eventual fail-closed watchdog settlement.
6. Undo or manually restore exact durable text and confirm the document becomes clean without another disk write; confirm canonical reload clears stale undo history while cancelled/failed transitions preserve usable history.
7. Move/detach a dirty tab between windows while editing and while Save/reload/reorder is queued. Confirm the target receives the latest canonical text, stale source requests reject, and no foreign projection is returned.
8. During detach, deliver concurrent ready notifications and then separately force timeout, load failure, and target close. Confirm all callers share one outcome, the target is destroyed once, and no ghost domain/main binding appears.
9. Save As A to B while Save/reload is queued and rapidly switch watched active tabs A to B while A stat is delayed. Confirm queued IO targets only B and no stale A watcher event or baseline wins.
10. Run the documented residue scans and confirm the old service, compatibility types, parameterless close/reload forms, direct transfer bypasses, raw renderer test owner, and consumer deep imports remain absent.

## Remaining Risks or Follow-ups

- RF-102 is dependency-ready but not started. It owns explicit workspace application ports/use cases and final save-state ownership without weakening RF-101 serialization or owner-CAS invariants.
- RF-201 owns the persistent CodeMirror-backed `TextBuffer`; the current immutable string implementation is the stable production contract, not a compatibility shim.
- RF-202 through RF-204 own revisioned edit IPC and deletion of full-draft synchronization.
- RF-401 through RF-404 own the recursive parser and incremental document-structure cache required for long-file editing performance.
- The formal behavior baseline still records 534 known product defects assigned to later roadmap tasks. RF-101 adds zero unexpected behavior in the final clean acceptance run.
- The one preceding behavior attempt exposed the established focus-derived active-line visibility timing flake. It did not alter source/semantic results and was followed by a complete clean run; runner settlement diagnostics remain separate follow-up work.
- Live macOS process, filesystem-identity, IME, cursor, and native-window manual acceptance remains part of later cross-platform/final gates.

## Record Scope

The editor-foundation program uses `docs/refactor/editor-foundation/roadmap.md`, `docs/refactor/editor-foundation/progress.md`, `docs/test-report.md`, and this summary as its RF ledger. `MVP_BACKLOG.md` and general `docs/progress.md` contain no RF task ledger and were intentionally not modified. This acceptance changes documentation/status records only, starts no RF-102 implementation, includes no ignored `dist` or `.artifacts` output, and does not push the branch.
