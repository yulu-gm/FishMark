# RF-001 Editing Behavior Baseline

## Result

`PASS` — architecture acceptance and task acceptance both passed on 2026-07-14.

## Completed

- Established one typed, executable corpus for Enter, Backspace, Tab, Shift+Tab, ArrowUp, ArrowDown, selection, repeat, and undo across the ten recursive container paths and representative semantic depths 0 through 8.
- Re-executes all 121 cases and 2,541 checkpoint/aspect targets in one sandboxed test-only Electron `BrowserWindow` and one reusable CodeMirror `EditorView`.
- Separates the desired contract from 534 exact typed current-product defect observations; no current defect was promoted to desired behavior.
- Enforces execution, desired-contract, run, calibration, target, and observed-value identity before exposing the sanitized plan.
- Routes the public `electron-batch` scenario capability to the same atomic formal gate and fails closed for unknown filters or missing adapters.
- Uses bounded Windows descendant termination and POSIX process-group cleanup, and restores the external abort-listener baseline after all 121 scenario steps.
- Keeps raw reports ignored under `.artifacts/editor-behavior/`; no compatibility facade, screenshot, generated baseline artifact, product API, or dead runner path was accepted.
- Changed no product editor command, parser, decoration, save, IPC, or user-visible editing behavior.

## Fresh Validation

- `npm.cmd run test:editor-behavior`: `PASS`; 121/121 cases, 2,541/2,541 targets, 79 verified-existing, 1,928 verified-runner, 534 known-defect-observed, 0 unexpected, 0 not-run, 24.573s.
- `npm.cmd run test:scenario -- --id editor-behavior-matrix --step-timeout 180000 --no-artifacts`: `PASS`; 121/121 steps, one formal batch, 25.351s.
- `npm.cmd run test -- packages/test-harness src/renderer/editor-test-driver.test.ts`: `PASS`; 27 files, 165 tests.
- `npm.cmd run test:editing-experience`: `PASS`; 79 cases, `failures: []`.
- `npm.cmd run lint`: `PASS`; 0 errors and 8 pre-existing unused-disable warnings.
- `npm.cmd run typecheck`: `PASS`.
- `npm.cmd run test`: `PASS`; 135 files, 1,487 tests.
- `npm.cmd run build`: `PASS`; existing Vite chunk-size warning remains non-blocking.
- `git diff --check`: `PASS`.
- Independent architecture acceptance of `9ae314d..86af99e`: `PASS`; no blocking architecture, security, compatibility, or dead-path finding remained.

## Manual Acceptance

1. Run the full formal command and confirm the summary is 121/121 cases, 2,541/2,541 targets, 0 unexpected, and 0 not-run.
2. Run the exact case, command, and container-path filters from the handoff; then pass an unknown value and confirm it exits nonzero instead of selecting an approximate case.
3. Inspect the top-level Shift+Tab case and confirm it is a no-op; inspect quoted nested Shift+Tab and confirm initial/undo contain both list layers while the primary result contains the legal outdent.
4. Inspect one primary/repeat/undo trace and confirm each branch reconstructs its declared ancestry, hard-resets the view, uses real keyboard events, and performs undo through Mod-z.
5. Inspect the Electron test window and observer boundaries: sandbox and context isolation are enabled, Node integration and child windows are disabled, and the observer imports no desired result, Typora oracle, or current-observation baseline.
6. Confirm `.artifacts/editor-behavior/` remains ignored and `git status` contains no report JSON, screenshot, compatibility facade, or generated calibration artifact.
7. Open the product editor for a short smoke check and confirm ordinary editing is unchanged; RF-001 adds a gate and does not alter product runtime behavior.

## Remaining Risks or Uncovered Work

- The 534 exact known-defect observations are real current editor behavior gaps around recursive parser paths, nested code/math line roles and geometry, and structural command/source/selection behavior. They are inputs to later RF tasks, not missing RF-001 executions.
- This fresh acceptance ran on Windows. The POSIX cleanup branch is covered by deterministic tests; live macOS editing, IME, geometry, and process acceptance remains scheduled by the later roadmap gates.
- RF-001 freezes behavior evidence only. Incremental document caching, recursive parser replacement, pure semantic commands, the thin CodeMirror adapter, performance budgets, E2E data safety, and final dead-code purge remain future roadmap work.
