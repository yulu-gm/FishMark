# RF-202 Shared Edit Contract and Main Handler

Task: `RF-202`

结果：`PASS` — independent architecture review and formal task acceptance both passed on 2026-08-04.

## 完成内容

- Added structured-clone-safe shared apply, flush, result, and document-projection contracts with revision metadata on workspace snapshots.
- Added owner-aware domain/application edit admission, contiguous per-client sequence handling, typed duplicate/gap/conflict/invalid results, and a real `throughSequence` flush barrier.
- Serialized apply and flush through the same per-tab coordinator and made live sender authorization mandatory inside its critical section before any domain read or mutation.
- Added focused main IPC handlers that reconstruct every wire result explicitly, publish owner-only metadata projections for applied batches, and expose canonical Markdown only for explicit revision conflict.
- Replaced the inline preload object with one complete `createProductApi` builder and added a fail-closed canonical-symbol/runtime-construction-owner architecture invariant.
- Retained the renamed full-draft path only as explicit RF-204 deletion debt; RF-202 added no feature flag, second canonical owner, or permanent compatibility structure.

## 验收依据

- Independent architecture review: `PASS`, P0/P1/P2 = 0, no open questions.
- Final quality review: Critical 0, Important 0, Minor 1, `Ready: Yes`.
- Valid batches advance canonical revision once; duplicate retries are idempotent; gaps, malformed payloads, stale revisions, ownership changes, and overflow have typed results.
- A sender that loses tab ownership while queued cannot mutate content/revision/high-watermark, read the edit checkpoint, or receive a projection.
- Normal acknowledgements and events contain no full Markdown; conflict recovery alone returns exact canonical text.
- Shared DTOs contain no Electron, CodeMirror, domain, or application types, and application/domain remain runtime-neutral.

## 验证

- Fresh focused RF-202 gate: 24 files / 529 tests passed.
- `npm.cmd run test:editor-foundation`: 7 files / 307 tests passed.
- `npm.cmd run lint`: passed with 0 errors / 8 existing warnings.
- `npm.cmd run typecheck`: passed.
- `npm.cmd test -- --reporter=dot --silent=passed-only`: 172 files; 2,232 passed plus 1 explicit skip out of 2,233 total.
- `npm.cmd run build`: renderer, Electron, CLI, and all three workspace runtime verifiers passed; the existing `>500 kB` advisory remains.
- Formal `npm.cmd run test:editor-behavior`: 121/121 cases and 2,541/2,541 targets; 79 existing, 1,928 runner, 534 known defects, 0 unexpected, 0 not-run; 25,869 ms.
- `git diff --check`: passed; output contained line-ending warnings only.

## 人工验收

1. Launch the built editor, create an untitled tab, enter Unicode and mixed newline content, save it, and confirm the currently active full-draft editing/save behavior remains unchanged before the RF-203/RF-204 cutover.
2. Inspect an active workspace snapshot and verify `activeDocument.revision` and `savedRevision` are present and coherent.
3. Subscribe to `onDocumentProjection`, apply one valid batch with a fresh client ID and sequence 1, and confirm the acknowledgement and event contain revision/dirty metadata but no document body.
4. Retry the same sequence with stale `baseRevision` and malformed `changes`; confirm it returns `duplicate`, does not reapply text, and emits no second projection.
5. Submit the next sequence against a stale revision; confirm only `revision-conflict` returns exact `canonicalText`.
6. Flush through sequence 0 and the acknowledged sequence, then flush beyond the high-watermark; confirm the first two succeed and the last returns the exact sequence gap. Repeat with another client ID to confirm independent checkpoints.
7. Queue an apply or flush, move the tab to another window before it acquires the per-tab slot, and confirm the stale owner receives `tab-owner-changed`, causes no mutation/checkpoint read, and receives no foreign projection.
8. Exercise existing edit, Save, Save As, reload, switch, move/detach, and close flows to confirm the formal editing baseline remains unchanged.

## 剩余风险或未覆盖项

- RF-203 owns renderer client identity, animation-frame batching, pending queues, `flushEdits()` integration, conflict rebase/recovery, and client lifecycle retirement. It is dependency-ready `PLANNED` and has not started.
- RF-204 owns deletion of the old full-draft channel and snapshot-preservation path. Until that task, this path remains only as explicit deletion debt and must not expand into a compatibility layer or second document owner.
- The non-blocking quality-review Minor is that non-local, cyclic, and reassigned fail-closed ProductBridge-analysis branches lack direct parameterized tests; the guard remains fail-closed and the task is ready.
- The 8 existing lint warnings, the existing Vite large-chunk advisory, and 534 calibrated known behavior defects remain unchanged. RF-202 claims no timing-performance budget.

## 状态

- RF-202: `COMPLETE`
- Result: `PASS`
- M2: 2 / 4, `IN_PROGRESS`
- Program: 6 / 38 tasks complete
- Current task: none
- Next dependency-ready task: RF-203 (`PLANNED`, not started)
