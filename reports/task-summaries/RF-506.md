# RF-506 — 生产语义引擎硬切换

状态：COMPLETE / PASS。2026-09-19 父独立行为/安全验收 PASS；2026-09-22 最终 CI Quality 与原始 bundle 预算全部 PASS，RF-506 与 M5 正式收口。

本轮 intake：`docs/plans/2026-09-19-rf-506-intake.md`。旧的五十八次临时切换、失败与回滚记录见 `docs/plans/2026-09-18-rf-506-handoff.md`，不代表当前生产路径。

## 已落地的生产变化

- 键盘、格式命令、表格编辑与测试入口统一调用 editor-model，经 codemirror-adapter 应用计划；不存在旧语义 fallback。原生菜单只有已有文件/窗口命令，不虚构格式菜单消费者。
- 单个 renderer update listener 接收所有实际文本变更，保留既有队列、会话和持久化 owner；adapter 不新建第二条传输路径。
- 修正 canonical 嵌套容器、物理前缀顺序、命令/光标和表格边界；删除旧命令规划器、粗 parity 豁免测试以及无消费者的旧语义上下文。
- 显示投影复用 canonical tree，物理行与 outline 按不可变文档复用；二分索引替代逐行扫描。修复普通段落表格探测的重复后缀扫描。
- 新增真实 controller 合同、解析差分、实际 Electron 行高/折叠检查。组合输入、Unicode 删除修复由真实入口验证，不仅测纯函数。

## 正式行为与校准证据

父独立运行 `.artifacts/rf506-parent-contract-final.json`：121/121 场景，2541 个检查点目标，2434 精确满足合同、107 个严格保持原值的历史缺陷，unexpected=0、not-run=0。真实 run ID 为 `d46d7078-8ac3-4cf3-9a0f-0ac89953a320`。

manifest `fnv1a32-c3f2d4a4`、contract `fnv1a32-c680cf8b`、最终 calibration `fnv1a32-12fd6881`。历史 `current-observations.ts` 未改；active calibration 无 pending。每个保留 known 值都与历史观测严格相同，没有新增豁免或放宽比较器。

夹具合同修订经过独立规范或既有生产证据核对：空列表没有虚构段落；合法深层任务列表使用多行嵌套；容器外尾随空白不继承深度；三个手工推导的引用 Enter 检查点按既有源码合同修正。显示折叠要求保留。这些修订不是声称重新捕获了 Typora exact 结果。

## 性能与最终门禁

父真实 Electron 探针：5k 行打开 165.6ms、输入 dispatch p95 19.7ms、selection p95 3.4ms；20k 行打开 424.9ms、输入 p95 74.6ms、selection p95 12.9ms。两档各 30 次编辑均源码精确、30 个变更帧。两次 RAF 仅为渲染机会代理，不能称原生输入到 compositor 绘制延迟。详见 `.artifacts/editor-runtime-performance.json`（含机器与 Electron 环境）。

最终包体积预算 PASS，且四个原始上限均未放宽：max initial chunk 179699/300000B、max initial chunk gzip 56955/90000B、total initial gzip 93918/260000B、total JS gzip 1425457/1430000B。`perf:bundle` 改为 Vite hidden sourcemap：仍生成完整 `.map` 供 provenance/架构合同验证，但不把仅用于分析的 `sourceMappingURL` 注释计入生产 JS；正式 `build:renderer` 本来也不发出该分析注释。KaTeX/Mermaid forbidden-initial 与 required-lazy 合同继续 fail-closed。

列表几何探针修复了隐藏窗口 RAF 挂起，并增加进程超时。当前唯一失败为尾随三个空格光标偏移 11.578px（期待 0±1px）；父在未改动 fdddabd worktree 使用同一窗口独立运行得到完全相同失败，非本轮回归。保留原断言，M6 必须明确空白展示与光标合同。记录 `.artifacts/rf506-list-geometry-final.log`、`rf506-list-geometry-baseline.log`。

## 后续验收

父最终冻结行为验证保持有效：全量 2671 passed / 1 skipped / 0 failed（.artifacts/rf506-parent-stable-full.json）；正式行为 121/121、2541 targets、2434 exact / 107 unchanged known / 0 unexpected / 0 not-run（.artifacts/rf506-parent-stable-behavior.json）；editing-experience pass=true；600 次连续随机编辑 differential/invariants 通过。2026-09-22 GitHub CI 在最终生产 bundle 合同上补齐收口证据：Quality 的 typecheck、lint、347 个 focused architecture/regression tests、build 与 clean-tree check 全 PASS；Bundle budget 全 PASS。RF701/RF602/RF603/RF604 以及 RF702/RF703 已在 M6/M7 完成，因此 RF-506/M5 不再有未完成 gate。

## 人工验收

1. 在引用、列表、表格间反复 Enter、Backspace、Tab、方向键并撤销，核对源码与焦点。
2. 使用中文输入法在有序列表中输入，候选阶段文本不被改写；结束后编号、撤销和保存正确。
3. 删除 emoji、组合音调和 ZWJ 字符，前向/后向删除均不留下破损字符。
4. 打开 20k 行文档，输入、选择与滚动，检查光标可见及预览稳定。

仍不由 M5 声称覆盖：真实 OS IME 跨平台手测、最终原生输入到绘制预算，以及 M9 的完整性能/E2E/安全矩阵。这些属于后续里程碑，不阻塞 RF-506/M5 完成。
