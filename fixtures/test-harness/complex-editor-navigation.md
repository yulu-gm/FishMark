# 一级标题

普通段落第一行，包含一些用于换行观察的中文内容，但这里按源码行来定义导航预期。
普通段落第二行，紧跟着测试标题和列表之间的连续移动。

## 二级标题

- [ ] 待办事项
- [x] 已完成事项
- 普通列表项

1. 有序列表第一项
2. 有序列表第二项
3. 
4. 有序列表第四项

> 第一条引用内容，用来测试从下方空行向上进入引用块时应该落到最后一条源码行。
> 第二条引用内容，包含 `inline` 与 **bold**。
> 第三条引用内容，后面要验证 Backspace 和 ArrowUp 的跨行行为。

```ts
export function sum(a: number, b: number): number {
  return a + b;
}
```

分割线上方的普通段落。

+++

## 表格区

| name | qty | note |
| :--- | ---: | :--- |
| pen | 2 | alpha |
| book | 5 | beta |
| ink | 1 | gamma |

表格下方的普通段落第一行。
表格下方的普通段落第二行。

### 尾部标题

`内联代码`
- ~~删除线任务~~

