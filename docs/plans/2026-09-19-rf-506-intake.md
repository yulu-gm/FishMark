# RF-506 continuation intake

日期：2026-09-19。用户授权通过 SubAgent 持续推进至 M5、M6 完成。

Task: RF-506（当前唯一实现任务）；后续按 RF-701 → RF-602 → RF-603 → RF-604 执行。

Goal: 新语义引擎在真实生产入口取代旧语义引擎，保持编辑、撤销、组合输入、数据传输与几何行为；完成后推进薄适配层。

In scope: 主目录已有 RF-601/RF-506 未提交改动；永久生产接线；解析/纯模型根因修复；五类入口的执行证据；旧语义删除；新路径真实性能与完整行为验证。M6 所需 RF-701 共享 render plan 属于必要前置工作。

Out of scope: 替换技术栈、云同步、以 known deviation 降低切换标准、重写既有断言以接纳回归。不得回滚已有工作或用临时切换后回滚作为交付。

Landing area: runtime agent 独占 editor-core/renderer/adapter 接线；model agent 独占 editor-model/markdown-engine；只读 reviewer 审核 adapter 风险和 M6 迁移边界；父 agent 负责共享计划、集成与独立验收。

Acceptance: 生产键盘/菜单/工具栏/table widget/test driver 到达唯一新语义引擎；默认运行时 corpus 与全部正式检查点通过；不把测试允许的 parity 差异当作已完成；无旧路径 fallback，旧模块无调用者后删除；没有第二套状态/队列；真实新路径延迟与扫描量可审查。

Verification: 聚焦 model/parser/adapter/renderer 回归 → 架构 guard → lint/typecheck/full Vitest/build → 正式 Electron behavior、editing/geometry 与候选新路径性能。父 agent 使用新鲜结果独立验收；真实 OS IME/平台不可执行部分明确标注，不冒充自动化覆盖。

Risks: 隐藏标记与光标归一化、嵌套容器、事务过滤器二次改写、undo 分组、组合文本、重复 frame、布局改变与 viewport 循环、长文档全文派生。

Doc updates: MVP_BACKLOG、design、test-cases、decision-log、progress、roadmap、test-report 和 task summaries。历史失败保留为历史，不重复标为当前阻断。

Next skill: fishmark-task-execution；完整切片后 fishmark-architecture-acceptance / fishmark-task-acceptance。
