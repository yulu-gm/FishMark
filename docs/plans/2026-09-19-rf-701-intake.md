# RF701 / M6 执行边界

日期：2026-09-19。用户授权以 SubAgent 完成 M5/M6，父 agent 独立验收。

## 前置与计划调整

RF506 已永久切换生产语义路径。待其最终行为、安全及构建门禁稳定后进入本任务；其包体积预算目前 FAIL，最终 M5 验收继续保持 pending，不将尚未通过的门禁标为通过。新旧显示派生并存的移除依赖本任务，因此允许顺序推进实现并在 M6 完成时共同验收原始预算。

本文件为后续 intake，当前并未表示 RF701 已实现。

## RF701 范围

1. canonical engine 补齐表格单元格 inline 与有效脚注定义/正文分段等必要显示元数据，保留源范围及增量差分一致性。语义解析只由 engine 拥有。
2. markdown-presentation 纯 render plan 遍历任意容器与叶，引用 canonical IDs、范围、inline、marker 和 capability。共享层不接收选区、焦点、viewport、DOM、CSS 或 source-view 状态。
3. 同一 revision 的真实编辑器消费者读取该 plan，完整构建与选区局部更新一起切换。列表项必须允许引用、围栏、数学、表格与多段正文；不再只递归 ListBlock children。
4. 编辑器活动态、表格位置、物理行与隐藏标记查询使用同一 snapshot，停用生产 rich-document 再派生。仍被 export/CLI 使用的兼容输出只能无损映射 canonical 元数据；移除重新扫描 list scopes/inline 和猜测层级的投影规则。

## 随后 M6

- RF602：装饰由 revision/snapshot/plan 驱动。影响行高的 widgets/replace 直接 StateField 提供；仅布局安全的可视区域标记走 viewport。选区、焦点与滚动不重解析，普通局部输入不重建全文装饰。跨行 inline 按完整叶语义裁剪。
- RF603：现有 interaction registry 按 typed capability 接入；table DOM focus 与 semantic edit 分离；异步图片/math/Mermaid/highlight 对 session/revision/disposal 验证后更新并 requestMeasure。
- RF604：工厂和实际 CM 代码归 adapter，纯模型/展示 helper 归合法 owner；删除 editor-core 及所有旧引用、别名和例外。无需借此次工作宣告 export/outline/metrics 的整个 M7 已完成。

## 验收

沿用源文本、选区、undo、composition、frame、正式 Electron 校准、真实 DOM 几何和 source mode 合同。跨任意容器的叶必须真实显示且保留编辑入口。禁止以旧有损投影输出充当独立正确性 oracle，禁止新增 known 来接纳迁移回归。

继续原 bundle 预算与真实 Electron 5k/20k 延迟记录；分清 parser、plan、decorations 和布局成本。保留真实 OS IME/跨平台和原生 input-to-paint 未测限制。每个切片有生产调用与退休旧路径，避免无人消费的 sidecar。

列表几何探针已有尾随三个空格光标偏移问题，父在 fdddabd 原版本独立复现同值；本任务需明确其源字符可见性与光标合同，不能直接把失败改成新的 expected 像素。

完整结束条件：M5/M6 相关代码、测试、文档一致，build/lint/typecheck/full tests/formal behavior 通过，原包体积预算通过，editor-core 真实删除，父独立架构和任务验收。没有达到这些条件不标目标完成。
