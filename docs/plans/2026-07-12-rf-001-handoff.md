# RF-001 Editing Behavior Baseline Execution Handoff

Date: 2026-07-14
Task: RF-001
Status: COMPLETE — architecture and task acceptance passed on 2026-07-14
Branch: `codex/editor-foundation-refactor`

## Outcome

RF-001 now has one accepted executable behavior corpus and a real Electron gate. All 121 cases and 2,541 checkpoint/aspect targets are re-executed against CodeMirror; the formal acceptance run completed in 24.573 seconds with 79 repository/FishMark matches, 1,928 runner-calibrated matches, 534 exact known-defect observations, zero unexpected mismatches, and zero not-run targets.

The canonical manifest contains no evidence gaps. Existing repository/FishMark provenance is retained; 2,007 explicitly enumerated desired targets and 534 exact defect targets form a disjoint, complete 2,541-target calibration set. Runner-confirmed desired targets carry Electron provenance; current mismatches carry `known-defect-observed` provenance and an aspect-specific exact structural value in `fixtures/editor-behavior/current-observations.ts`. Desired results remain unchanged, so current defects are not adopted as product behavior.

The quoted nested-list Shift+Tab contract now correctly records depth 3 and the full nested semantic path for initial/undo while retaining the one-list path after outdent. Its actual truncated parser-path observation remains an exact known defect rather than being promoted to desired behavior. No product editor command, parser, decoration, save, IPC, or user-visible behavior changed.

## Runtime Design

- `createExecutionPlan()` removes desired results, classifications, evidence, and container metadata from the renderer payload. `manifestHash` covers executable IDs, initial state, checkpoint ancestry, and actions; the independently checked `contractHash` additionally covers every desired checkpoint result. Neither hash covers evidence or current-defect baselines.
- `corpus.ts` owns canonical composition. The formal renderer must enter through `formal-run-port.ts`, which fail-closes on stale execution/contract/calibration identity or incomplete target coverage before exposing a sanitized execution plan and owner-held comparison closure.
- The launcher accepts exact `--case`, `--command`, and `--container-path` filters, rejects unknown inputs, enforces a 180-second hard timeout, and atomically writes ignored JSON reports under `.artifacts/editor-behavior/`.
- One sandboxed/context-isolated `BrowserWindow` owns one reusable CodeMirror `EditorView`. The Electron main process measures `BrowserWindow.getAllWindows()` before accepting the report and denies child-window creation. Test-only Electron launchers share one paintable-offscreen configuration so animation frames are not background-throttled and no taskbar window is shown.
- Every case/branch hard-resets source, selection, view mode, and undo history. Primary runs from initial, repeat from the actual primary result, and undo reconstructs its declared ancestors before dispatching real bubbling/cancelable Mod-z.
- Insert and selection actions use the public controller. Keyboard actions use real `KeyboardEvent`s; no direct command fallback or case-ID behavior branch exists.
- The observer independently reads live state, parser paths, source offsets, physical line roles/geometry, DOM computed style/rects, and view mode. Runner and observer architecture tests forbid imports of desired-result/physical-expectation helpers.
- The comparator emits exactly one of `verified-existing`, `verified-runner`, `known-defect-observed`, `unexpected-mismatch`, or `not-run` for every target. Exact current observations are target-level structural baselines, never case-wide allowlists.
- Verdicts and defect observations are discriminated by aspect, so command plans, semantic paths, source, selection, line roles, geometry, and view mode cannot be interchanged. Runtime validation rejects untyped malformed input.
- `calibrationHash` deterministically covers both plan hashes, the exact run ID, and sorted verified/defect target-value sets. Reusing a baseline with a different run, contract, target, observed value, or reason fails before composition.
- The Typora conversion gate reads the real catalog and capture JSON, maps the full captured action sequence to one primary/repeat checkpoint, and records exact versus unavailable coverage for initial/actions/primary/repeat/undo fields.
- The public `test:scenario` CLI dispatches the `electron-batch` capability to the same formal gate. The first scenario step runs the complete batch once; all later steps share that atomic result.
- A shared Node process-tree controller owns its descendant registry, retains detached POSIX process-group ownership after the root exits, excludes that exited root from individual PID signals, and escalates a still-live group from TERM to KILL at half of the 6-second cleanup budget. Windows uses `taskkill /T /F`; each terminator has a 2-second budget, and a hung terminator is force-signalled and then exit/close-confirmed within the same global deadline. An unconfirmed terminator or stubborn POSIX group is an explicit cleanup failure. The CLI has a finite 10-second post-abort fallback and reports cleanup cutover explicitly.
- The scenario runner installs one disposable external-abort listener per step. A 121-step run returns the signal listener count to its baseline instead of accumulating dormant `once` listeners.

## Canonical Files

- Corpus and composition: `fixtures/editor-behavior/manifest.ts`
- Canonical composition owner: `fixtures/editor-behavior/corpus.ts`
- Formal renderer support port: `fixtures/editor-behavior/formal-run-port.ts`
- Calibration-independent corpus: `fixtures/editor-behavior/raw-cases.ts`
- Model/evidence validation: `fixtures/editor-behavior/model.ts`
- Sanitized executable projection: `fixtures/editor-behavior/execution-plan.ts`
- Observation/report/comparison protocol: `fixtures/editor-behavior/runner-protocol.ts`
- Exact current baseline: `fixtures/editor-behavior/current-observations.ts`
- Renderer executor: `src/renderer/editor-behavior-manifest-runner.ts`
- Independent observer: `src/renderer/editor-behavior-observer.ts`
- Node/Electron launchers: `scripts/probe-editor-behavior.mjs`, `scripts/editor-behavior-process-launcher.mjs`, `scripts/electron-editor-behavior-main.cjs`, `scripts/process-tree.cjs`
- Shared paintable test window: `scripts/electron-test-window.cjs`
- Capability dispatch: `packages/test-harness/src/handlers/capability.ts`, `packages/test-harness/src/handlers/editor-behavior-batch.ts`

## Fresh Verification

```text
npm.cmd run test:editor-behavior
PASS — 121/121 cases, 2,541 targets, 79 verified-existing,
1,928 verified-runner, 534 known-defect-observed, 0 unexpected, 0 not-run, 24.573s.

npm.cmd run test:scenario -- --id editor-behavior-matrix --step-timeout 180000 --no-artifacts
PASS — public capability path, 121/121 scenario steps, one formal batch, 25.351s; real launcher timeout, adapter pre-abort/abort/nonzero, public-CLI abort/timeout, root-exited descendant, and injected hung-Windows-terminator boundaries also pass.

Exact filter smoke:
--case empty-type-hash: 1/1 cases, 21 targets, PASS.
--command Tab: 15/15 cases, 315 targets, PASS.
--container-path "Document > Blockquote > List > ListItem > Paragraph":
13/13 cases, 273 targets, PASS.
Unknown case: exit 1 with an explicit unknown-case error.

npm.cmd run test -- packages/test-harness src/renderer/editor-test-driver.test.ts
PASS — 27 files / 165 tests.

npm.cmd run test:editing-experience
PASS — 79 cases, failures: [], exit 0.

npm.cmd run lint
PASS — 0 errors; 8 pre-existing unused-disable warnings.

npm.cmd run typecheck
PASS — renderer, electron, vitest, and CLI projects.

npm.cmd run test
PASS — 135 files / 1,487 tests.

npm.cmd run build
PASS — renderer/electron/CLI; existing Vite chunk-size warning remains.
```

Raw report JSON is intentionally not committed. The typed calibration records execution manifest hash `fnv1a32-fbeac900`, desired contract hash `fnv1a32-3f037eee`, calibration identity `fnv1a32-2a007600`, and fresh calibration run ID `d7705d03-b141-46dd-8b4b-4559024c14bc`.

## Acceptance

- Architecture acceptance: `PASS` over `9ae314d..86af99e`; no blocking architecture, security, compatibility, or dead-path finding remained.
- Task acceptance: `PASS` on 2026-07-14 with fresh formal, public-scenario, focused, legacy editing, lint, typecheck, full-test, build, and diff gates.
- Acceptance record: `reports/task-summaries/RF-001.md`.

## Known Defect Interpretation

The 534 target mismatches cluster around current parser-path truncation for empty/list/mixed containers, nested list-owned code/math roles and geometry, and current Enter/Tab/Shift+Tab/Arrow source/selection behavior. They are future implementation work, not RF-001 gate omissions. Every mismatch is still rerun; any structural drift becomes an unexpected mismatch.

## Manual Acceptance

1. Inspect the execution plan and confirm desired results/evidence are absent.
2. Inspect runner/observer imports and confirm the runner enters through `formal-run-port.ts`, while neither executor nor observer consumes physical expectations, checkpoint results, Typora oracle data, or current-observation storage.
3. Run the full and three filtered commands above; verify unknown filters fail closed.
4. Run the formal-port stale calibration hash, wrong run ID, stale contract, and missing-target tests; confirm all reject before any execution data is returned. Then confirm the explicit verified and defect target sets are unique, disjoint, cover all 2,541 targets, and canonical evidence has zero gaps.
5. Inspect one primary/repeat/undo trace and confirm ancestry, hard reset, real keyboard event, and Mod-z behavior.
6. Confirm `.artifacts/editor-behavior/` is ignored and no raw report/screenshot is staged.
7. Confirm the product main/preload/renderer APIs and editor commands are unchanged.

## Next Skill

`$fishmark-task-intake` for `RF-002`.
