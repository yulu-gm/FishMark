# RF-001 Editing Behavior Baseline Execution Handoff

Date: 2026-07-14
Task: RF-001
Status: IN_PROGRESS — executable evidence runner pending
Branch: `codex/editor-foundation-refactor`

## Outcome

The RF-001 corpus is now a canonical, executable specification model. It does not claim that the product editor has executed every target, so RF-001 remains `IN_PROGRESS`.

All 121 cases contain a complete initial result and an ordered `primary -> repeat` timeline plus an explicit undo branch. Checkpoints carry executable discriminated actions (`press-key`, `insert-text`, `set-selection`, `undo`) and explicit ancestry; there are no operation-count-only expectations, compatibility fields, or case-ID action inference.

Evidence is recorded as one mutually exclusive `Gap | Verified` value for every checkpoint/aspect target. Probe and repository-test provenance is validated, and a pure `replaceEvidenceGaps` function compares typed observations before atomically replacing gaps and recomputing case status. Metadata-only scenario execution is declared as capability metadata rather than recognized by scenario ID.

No product editor command, parser, CodeMirror adapter, IPC, editor test driver, or runtime editing behavior changed in this slice.

## What Changed

- Moved the complete implementation to `fixtures/editor-behavior`; the test harness and renderer import this root-owned corpus directly. The former package implementation and root compatibility re-exports were deleted.
- Added complete initial/primary/repeat/undo results, checkpoint ancestry, and executable action sequences to every case. Each result owns its actual semantic path, including structural exit/outdent checkpoints. The compound whitespace/Enter/text case explicitly records both `Enter` and `insert-text("abc")`.
- Removed `viewModeContract`; each result records its actual `viewMode`.
- Replaced overlapping evidence arrays with `Record<checkpoint, Record<aspect, Gap | Verified>>`. Empty reasons, empty provenance, duplicate target declarations, incomplete matrices, and partial failed updates are rejected.
- Added typed observations and atomic evidence replacement for a later runner. A target becomes verified only when the observation equals the canonical expected value.
- Audited all 33 named probes from their executable `pass` conditions. The catalog now owns conservative checkpoint/aspect capabilities and probe function provenance; verified probe evidence must match its exact catalog target and assertion. An empty capability list is valid metadata and never creates empty verified evidence.
- Removed geometry overclaims from code-fence, deep ordered-list, raw-prefix, table, and other partial DOM probes. No named probe currently claims semantic path, physical geometry, view mode, command plan, or undo.
- Added exact source text to every physical-line record and strict validation for line order, nonnegative depth/columns, source bounds, marker/content relationship, and source-line consistency.
- Made fenced-code and block-math bodies opaque after stripping only their established outer quote/list prefix. Cross-language delimiters and list-like body text no longer create nested semantics, while the closing delimiter restores ordinary parsing for the following line.
- Added explicit scenario execution capability. The matrix is `metadata-only`; the existing fixture-picker limitation is declared on its step rather than hard-coded by scenario ID.
- Hard-cut the CLI TypeScript layout to repository-root ownership: `tsconfig.cli.json` compiles both the harness and root fixtures with `rootDir: "."`. The single CLI entry is now `dist-cli/packages/test-harness/src/cli/bin.js` in package scripts and main-process development wiring. The installer continues to exclude `dist-cli` by design.

## Canonical Files

- Corpus composition: `fixtures/editor-behavior/manifest.ts`
- Model, validation, action/evidence helpers: `fixtures/editor-behavior/model.ts`
- Named probe catalog and capabilities: `fixtures/editor-behavior/fishmark-probe-catalog.ts`
- Named probe cases: `fixtures/editor-behavior/probe-cases.ts`
- Recursive and representative cases: `fixtures/editor-behavior/nested-containers.ts`
- Harness scenario adapter: `packages/test-harness/src/scenarios/editor-behavior-matrix.ts`
- Model-quality tests: `packages/test-harness/src/scenarios/editor-behavior-model-quality.test.ts`
- Matrix tests: `packages/test-harness/src/scenarios/editor-behavior-matrix.test.ts`
- Executable probe registry binding: `src/renderer/markdown-editing-experience-probe.ts`

## Fresh Verification

```text
npm.cmd test -- --run packages/test-harness/src/scenarios/editor-behavior-model-quality.test.ts packages/test-harness/src/scenarios/editor-behavior-matrix.test.ts
Test Files 2 passed (2)
Tests 31 passed (31)

npm.cmd test -- --run src/main/package-scripts.test.ts
Test Files 1 passed (1)
Tests 31 passed (31)

npm.cmd run typecheck
Passed (renderer, electron, vitest, and CLI projects).

npm.cmd run build:cli
Passed.

npm.cmd run test:scenario -- --id app-shell-startup --no-artifacts
Scenario app-shell-startup passed; the new emitted CLI entry was used.

npm.cmd run lint
Passed with 0 errors and 8 pre-existing unused-disable warnings.

npm.cmd run test
Test Files 120 passed (120)
Tests 1399 passed (1399)

npm.cmd run build
Passed; the existing Vite chunk-size warning remains.
```

`npm.cmd run test:editing-experience` was not repeated. No probe case function or probe expectation changed; the renderer change is the canonical catalog import path. The earlier long probe run remains historical regression evidence and is not used as proof for the new checkpoint/aspect matrix.

## Remaining Evidence Gaps

The corpus has 121 cases and 2,541 checkpoint/aspect targets. It currently contains 79 verified targets and 2,462 explicit gaps. Thirty-two cases are partially verified, 89 are unverified, and no case is fully verified.

| Checkpoint | Verified | Gap |
| --- | ---: | ---: |
| primary | 67 | 780 |
| repeat | 12 | 835 |
| undo | 0 | 847 |

| Aspect | Verified | Gap |
| --- | ---: | ---: |
| command plan | 0 | 363 |
| semantic path | 0 | 363 |
| source | 33 | 330 |
| selection | 32 | 331 |
| visible-line roles | 14 | 349 |
| physical geometry | 0 | 363 |
| view mode | 0 | 363 |

The 33 named probes contribute 75 verified targets and two repository tests contribute four. All 70 generated parity cases and all nine representative depth cases remain fully unverified. The two known defects retain only their exact observed current result; their desired targets remain gaps.

## Minimum Next Runner Slice

Add one test-only manifest executor, without product IPC changes, that:

1. selects a case by stable ID and initializes its complete `initial` result;
2. executes checkpoint actions directly, without command-diff inference or case-ID branches;
3. reconstructs each checkpoint from its declared `from` state so repeat and undo branches are unambiguous;
4. returns typed source, selection, semantic path, physical line roles/geometry, and actual view mode;
5. captures the executed action trace so command-plan evidence is independently comparable;
6. passes typed observations to `replaceEvidenceGaps`, which updates evidence only after the entire requested observation set validates;
7. runs every required command/path pair before RF-001 can return to `DEV_DONE`.

Do not create a second corpus, duplicate editor commands, add compatibility paths, or report metadata-only steps as passes.

## Manual Review Draft

1. Inspect `empty-spaces-enter-text`; confirm its primary actions are `Enter` followed by `insert-text("abc")`.
2. Inspect one named-probe case with capabilities, one with an empty capability list, one parity case, and both known defects.
3. Confirm code/math body lines containing `$$`, backticks, or `- literal` remain opaque until the matching closing delimiter.
4. Confirm the former package-local implementation directory and all `operationCount` / `viewModeContract` compatibility fields are absent.
5. Confirm the matrix scenario is rejected through `execution.kind === "metadata-only"`, not its ID.
6. Confirm RF-001 remains `IN_PROGRESS`; formal acceptance is not next.

## Next Skill

`$fishmark-task-execution` for the executable evidence runner slice.
