# FishMark Editor Foundation Refactor Progress

**Program:** `REFACTOR-EDITOR-FOUNDATION`

**Roadmap:** `docs/refactor/editor-foundation/roadmap.md`

**Created:** 2026-07-11

**Last updated:** 2026-09-19

**Overall status:** `IN_PROGRESS`

**Current task:** `RF-506 DEV_DONE; final performance acceptance pending` — resumed 2026-09-19 under the user's M5/M6 goal. Runtime and pure-model SubAgents implement disjoint parts of this one task; the parent coordinates and independently accepts. Latest work is the uncommitted main checkout, not the unchanged refactor worktree.

**Next required skill:** finish RF-506 behavior/safety acceptance, then implement RF-701 shared render plan and RF-602/603/604 sequentially. Plan adjustment: keep RF-506 final performance acceptance open while M6 removes duplicate projection/derived code; require the unchanged bundle budget at combined completion. Do not falsely mark M5 COMPLETE to unlock dependencies.

**Current gate:** parent fresh frozen-code validation passed 2671 tests plus 1 existing skip; lint (0 errors / 8 existing warnings), typecheck, build and editing experience passed. Formal Electron: 121/121 cases, 2541 targets (2434 exact, 107 unchanged historical defects, zero unexpected/not-run). Unicode and IME review findings are repaired. Behavior/safety architecture PASS; bundle budget remains FAIL. RF-506 is DEV_DONE with final performance pending, M5 is not COMPLETE.

**Current performance evidence:** the same parent Electron controller probe improved 20k-line typing dispatch p95 from 1,934 ms to 66.9 ms and selection p95 from 1,697 ms to 13.9 ms after canonical-tree reuse and physical-range indexing. Source stayed exact and each of 30 edits emitted one frame. Two animation frames are a rendering-opportunity proxy, not compositor/native input timing. Full semantic/rendering budgets remain unproven; M6 must remove remaining full-document decoration work.
**User priorities:** maintainability/extensibility, editing/interaction quality, and measured performance. Preserve the existing stack and owners; do not optimize progress percentages, package count, or deleted lines.

## 1. Status vocabulary

Only these states are valid:

- `PLANNED`: scoped in the roadmap but implementation has not started.
- `IN_PROGRESS`: the task is the only active implementation task.
- `DEV_DONE`: implementation and focused verification are complete; formal acceptance is pending.
- `ACCEPTING`: architecture/task acceptance is running.
- `BLOCKED`: acceptance or implementation cannot continue without an external decision or state change; the blocker is written below.
- `COMPLETE`: implementation, deletion obligations, docs, required gates, and acceptance all passed.

At most one `RF-xxx` task may be `IN_PROGRESS` or `ACCEPTING` at a time. A later task cannot start until its dependencies are `COMPLETE`.

## 2. Program dashboard

| Milestone | Purpose | Status | Complete | Total | Blocking gate |
| --- | --- | --- | ---: | ---: | --- |
| M0 | Invariants and executable baselines | `COMPLETE` | 2 | 2 | Behavior matrix and architecture/performance baseline exist |
| M1 | Canonical workspace domain | `COMPLETE` | 2 | 2 | RF-101 and RF-102 accepted; domain and application boundaries are production dependencies |
| M2 | Revisioned edit transport | `COMPLETE` | 4 | 4 | RF-203 and RF-204 accepted; full-draft channel deleted |
| M3 | Data safety and recovery | `COMPLETE` | 4 | 4 | Inactive files protected; save/recovery/close are canonical |
| M4 | Recursive parser and incremental cache | `COMPLETE` | 5 | 5 | One recursive parser remains; differential cache tests pass |
| M5 | Pure semantic editing engine | `IN_PROGRESS` | 5 | 6 | All semantic commands migrated; old command engine removed |
| M6 | Thin CodeMirror adapter | `IN_PROGRESS` | 1 | 4 | Old `editor-core` package removed |
| M7 | Shared presentation and derived consumers | `PLANNED` | 0 | 3 | Editor/export/outline/metrics share canonical derived inputs |
| M8 | Renderer/main composition cleanup | `PLANNED` | 0 | 3 | React/main/preload are composition or presentation only |
| M9 | Performance, E2E, and security | `PLANNED` | 0 | 3 | Budgets, Playwright flows, and Electron security pass |
| M10 | Purge and final acceptance | `PLANNED` | 0 | 2 | No compatibility/dead code; final verdict `PASS` |

**Historical recorded completion:** 22 / 38 original tasks. The supplemental RF-HARDEN-001 repair gate is tracked separately; this count is not a current quality or release-readiness percentage.

## 3. Task ledger

Evidence columns are filled only with fresh command output/report paths from the task's own turn. A task cannot be marked `COMPLETE` with blank required evidence.

| ID | Task | Depends on | Status | Focused evidence | Full gates | Acceptance record | Commit/branch |
| --- | --- | --- | --- | --- | --- | --- | --- |
| RF-001 | Editing behavior baseline | — | `COMPLETE` | Canonical/formal runner, observer, protocol, launcher, identity, Typora conversion, listener, and process-tree boundaries accepted; fresh harness + driver: 27 files / 165 tests; public scenario: 121/121 steps in one batch. | Fresh formal Electron gate: 121/121 cases, 2,541/2,541 targets, 79 existing + 1,928 runner matches, 534 exact known defects, 0 unexpected, 0 not-run, 24.573s; public scenario 25.351s; editing experience 79/79; lint 0 errors (8 pre-existing warnings), typecheck, 135-file/1,487-test suite, build, and diff check passed. | Architecture `PASS`; task `PASS`; `reports/task-summaries/RF-001.md` | `codex/editor-foundation-refactor` |
| RF-002 | Architecture and performance guards | RF-001 | `COMPLETE` | Fresh public foundation gate 7 files/253 tests; fresh `perf:baseline` 23/23 contract checks and 7 files/253 tests. The manifest owns 73 exact CodeMirror targets; real micromark scan counters, resolved/case-folded repository paths, transparent dependency/site module arguments and literal `require` callees, and schema-v1 Vite module provenance close the reviewed evidence gaps. | Fresh lint 0 errors/8 pre-existing warnings; typecheck; 138-file/1,738-test suite; renderer/Electron/CLI build; exclusive formal behavior 121/121 cases and 2,541/2,541 targets with 79 existing + 1,928 runner + 534 known + 0 unexpected/not-run in 24.570s; ordinary dist 0 maps/0 provenance; diff check all passed. | Architecture `PASS` over `a6da237..53824a7`, 0 blocking findings/no open questions; task `PASS`; `reports/task-summaries/RF-002.md` | `codex/editor-foundation-refactor` at implementation head `53824a7` |
| RF-101 | Extract workspace domain | RF-002 | `COMPLETE` | Production `@fishmark/workspace-domain`; independent location/object ownership registries and typed ambiguous-owner detection; fail-closed filesystem identity resolution; one non-React renderer transaction application with canonical known/unknown admission, strict FIFO, generated per-tab outbox, exact `{tabId, epoch, loadRevision}` CodeMirror ownership, target/all-tab drains, tokenized sealing/sealed/releasing editor-transition acknowledgement, structural CodeMirror hard read-only, fresh-state canonical undo boundaries, recoverable epoch-only rebind, operation-local reload checkpoint, post-await disposal fences, invocation-bound save transactions, and presentation-only hooks; typed application-owned editor test adapter; shell state is a canonical-only projector with no draft merge/reload bypass; exact confirmation and main owner/active-tab/exact-window-instance CAS before focus; opaque active close-save lease capability; one `tab -> location -> object` transaction order shared by open/save/reload/close and transfer; typed reload stale/error results; stateless latest-intent watcher ownership; old main service, renderer hook transaction lanes, split save orchestration, raw test snapshot/mutation paths, shell-state draft compatibility, direct snapshot-push event, rollback activation, duplicate close-save facade, legacy test dialog facade, and dead APIs deleted. | Fresh acceptance: renderer/editing focus 13 files / 863 tests; roadmap domain/main focus 16 files / 217 tests; editor-foundation 7 files / 260 tests; escalated full Vitest 163 files / 2,052 passed + 1 explicit skip; typecheck, lint (0 errors / 8 existing warnings), build, final formal behavior 121/121 cases and 2,541/2,541 targets with 0 unexpected/not-run, residue scans, and diff check passed. | Architecture `PASS` over `f5aa70b..003d4d5`, P0/P1/P2 = 0 and no open questions; task `PASS`; `reports/task-summaries/RF-101.md`; execution handoff: `docs/plans/2026-07-16-rf-101-handoff.md` | `codex/editor-foundation-refactor`, accepted implementation head `003d4d5` |
| RF-102 | Extract workspace application ports/use cases | RF-101 | `COMPLETE` | Production runtime-neutral `@fishmark/workspace-application`; explicit consumed ports; typed edit/save/close/open/reload/reorder/transfer/detach/owner-activation/watch orchestration; exhaustive native-close application-to-shared DTO mapping; 11 superseded main-local business modules and obsolete same-name tests deleted; fresh focused gate 23 files / 561 tests. | Editor-foundation 7 files / 267 tests; lint 0 errors / 8 existing warnings; typecheck; full Vitest 165 files / 2,066 passed + 1 skip; build with both workspace runtime verifiers; formal behavior 121/121 cases and 2,541/2,541 targets with 0 unexpected/not-run; forbidden/residue scans and diff check passed. | Architecture `PASS`, P0/P1/P2 = 0; final quality Critical/Important/Minor = 0, `Ready: Yes`; task `PASS`; `reports/task-summaries/RF-102.md`; handoff: `docs/plans/2026-07-30-rf-102-handoff.md` | `codex/editor-foundation-refactor` |
| RF-201 | Persistent text buffer and session revisions | RF-102 | `COMPLETE` | Persistent CodeMirror `Text` production buffer behind runtime-neutral domain `TextBuffer`; explicit factory composition; revisioned/idempotent edit batches; contiguous client high-watermarks; malformed-input validation; public-entry/runtime guards and fail-closed source-root containment; handoff: `docs/plans/2026-08-04-rf-201-handoff.md`. | Post-fix domain/infrastructure 4 files / 163 tests; architecture 1 file / 209 tests; earlier cross-layer focus 63 files / 816 passed + 1 skip; editor-foundation 7 files / 285 tests; lint 0 errors / 8 existing warnings; typecheck; full Vitest 166 files / 2,162 passed + 1 skip; build with three workspace runtime verifiers; formal behavior 121/121 cases / 2,541/2,541 targets / 0 unexpected / 0 not-run; final acceptance-document diff check passed. | Specification/architecture `PASS`, P0/P1/P2 = 0 and no open questions; final quality Critical/Important/Minor = 0, `Ready: Yes`; task `PASS`; `reports/task-summaries/RF-201.md` | `codex/editor-foundation-refactor` |
| RF-202 | Shared edit contract and main handler | RF-201 | `COMPLETE` | Repository-owned edit/flush/projection DTOs; revision-bearing snapshots; owner-aware metadata-only domain edit/checkpoint entries; apply and through-sequence flush under the same per-tab coordinator with mandatory in-critical-section sender authorization; exact IPC result reconstruction; one complete preload product builder protected by a fail-closed TypeScript symbol/owner invariant. The renamed `updateDocumentDraft` channel remains only as explicit RF-204 deletion debt; handoff: `docs/plans/2026-08-04-rf-202-handoff.md`. | Fresh focus 24 files / 529 tests; editor-foundation 7 files / 307 tests; lint 0 errors / 8 existing warnings; typecheck; full Vitest 172 files / 2,232 passed + 1 skip out of 2,233; renderer/Electron/CLI build with all three workspace runtime verifiers; formal behavior 121/121 cases / 2,541/2,541 targets / 0 unexpected / 0 not-run; diff check passed with line-ending warnings only. | Architecture `PASS`, P0/P1/P2 = 0, no open questions; final quality Critical 0, Important 0, Minor 1, `Ready: Yes`; task `PASS`; `reports/task-summaries/RF-202.md`. | `codex/editor-foundation-refactor` |
| RF-203 | Renderer workspace client and pending queue | RF-202 | `COMPLETE` | Pure runtime-neutral pending queue and edit client (`src/renderer/application/`); RAF frame bucket with composition-aware seal; internal-origin remote patch; production hard-switch to revisioned frames with dormant full-draft only as RF-204 debt; non-conflict recovery auto-materialization; handoff: `docs/plans/2026-08-04-rf-203-handoff.md`. | Focused 6 files / 636 tests; editor-foundation 7 files / 308 tests; lint 0 errors / 8 existing warnings; typecheck; full Vitest 174 files / 2,413 passed + 1 skip; build exit 0; formal behavior 121/121 cases / 2,541/2,541 targets / 0 unexpected / 0 not-run on exclusive rerun. | Independent spec review 9/9 PASS; quality review Critical C1 fixed (non-conflict recovery auto-materialization) with regression tests; architecture `PASS`, P0/P1/P2 = 0; task `PASS`; `reports/task-summaries/RF-203.md`. | `codex/editor-foundation-refactor` |
| RF-204 | Full-draft synchronization hard cutover | RF-203 | `COMPLETE` | Deleted the full-draft transport end to end: shared channel/input type, preload bridge method, main IPC handler, workspace-application `updateDocumentDraft` use case and `drafts` port, renderer `WorkspaceDraftOutbox`/`recordEditorChange`/legacy drain paths; added an architecture guard forbidding the retired symbols. | lint 0 errors; typecheck; full Vitest 172 files / 2,372 passed + 1 skip; build exit 0; formal behavior 121/121 cases / 2,541/2,541 targets / 0 unexpected / 0 not-run. | `reports/task-summaries/RF-204.md` (to create) | `codex/editor-foundation-refactor` |
| RF-301 | Per-document watch registry | RF-204 | `COMPLETE` | New `src/main/infrastructure/file-watch-registry.ts` keys entries by normalized path with a per-window subscriber set, syncs every open tab path, suppresses own writes, and tears down shared watchers on last unsubscribe/destroy. `WorkspaceWatcherPort`/`syncWindow` now use `syncWindowPaths(context, tabPaths[])`. Deleted `external-file-watch-service.ts` and its test. Focused: registry 8 tests + workspace-application 19 + touched main integration 58 tests. | lint 0 errors / 8 pre-existing warnings; typecheck; full Vitest 172 files / 2,358 passed + 1 skip; build exit 0. | Self-acceptance: exit criterion verified (inactive-tab edits detected and delivered, own writes suppressed, shared watchers torn down); lint/typecheck/test/build green | `codex/editor-foundation-refactor` |
| RF-302 | Conflict-aware safe save | RF-301 | `COMPLETE` | New `src/main/infrastructure/document-repository.ts` (+ test) owns read+hash `readDiskVersion` and safe temp-write + atomic rename `writeDocument`; `DiskRepositoryPort` added; `save-document.ts` rejects a normal save with `disk-version-conflict` when the on-disk `contentHash` diverges and commits the post-write `DiskVersion`; `DiskVersion` is recorded at open/reload/save; `saveMarkdownFileToPath` deleted. Focused: repository 5 tests + save/open/reload/document-io/file-identity-races/window-close suites. | lint 0 errors / 8 pre-existing warnings; typecheck; full Vitest 173 files / 2,354 passed + 1 skip; build exit 0. | Self-acceptance: exit criterion verified (stale disk content rejected before overwrite, atomic replace, disk version threaded end to end). | `codex/editor-foundation-refactor` |
| RF-303 | Recovery journal and session restore | RF-302 | `COMPLETE` | Added `recovery-journal.ts` (+7), `workspace-persistence.ts` (+8), `recovery.ts` (+6), `recovery-service.ts` (+3), domain `exportSnapshot`/`restoreSnapshot` (+2, round-trip). Wired `main.ts`: journal every accepted edit, restore snapshot + replay journal on startup, compact to checksummed snapshot on `before-quit`, quarantine+log corrupt files. | lint 0 errors / 8 pre-existing warnings; typecheck; full Vitest 173 files / 2,354 passed + 1 skip; build exit 0. | Self-acceptance: exit criterion verified (recovery is main-owned and renderer-memory-free). | `codex/editor-foundation-refactor` |
| RF-304 | Main-owned conflict and close workflows | RF-303 | `COMPLETE` | Session-level `externalChange` + `markExternalChange`; main marks on watch events and projects it; `resolve-external-change.ts` (+3 tests) typed keep-memory/reload/save-as/cancel commands; `resolveExternalChange` IPC + preload; renderer derives conflict from projection and sends commands; deleted `useExternalConflictController.ts` (+test) and shell-state conflict reducers; autosave blocked by projected conflict. | lint 0 errors / 8 pre-existing warnings; typecheck; full Vitest 176 files / 2,377 passed + 1 skip (one pre-existing icon-timing flake passes in isolation); build exit 0. | Self-acceptance: exit criterion verified (conflict/close are main-owned and projected). | `codex/editor-foundation-refactor` |
| RF-401 | Recursive node model and source mapping | RF-304 | `COMPLETE` | Parser-agnostic recursive model in `packages/markdown-engine/src/model/`: `source-range.ts` (ranges, markers, masked/container-prefixed source, offset-preserving masking), `container-path.ts` (ancestry paths), `markdown-node.ts` (container/leaf unions, node data, constructors), `document-tree.ts` (FNV-1a node identity from ancestry + subtree fingerprint, tree index, navigation, invariants). 9 focused tests cover CRLF/tabs/Unicode/empty containers/lazy continuation/depth 0–8. Legacy block-map parser untouched. | typecheck; lint 0 errors; build exit 0; full Vitest. | Self-acceptance: model expresses every recursive parity path without top-level special cases. | `codex/editor-foundation-refactor` |
| RF-402 | Full recursive parser | RF-401 | `COMPLETE` | `packages/markdown-engine/src/parse/micromark-event-adapter.ts` flattens micromark events into offset-bearing views; `full-document-parser.ts` drives one container stack (`blockQuote`/`listOrdered`/`listUnordered` + synthetic `listItemPrefix` item frames) into the recursive model, unions lazy-continuation ranges, masks container prefixes before leaf inline parsing, and builds reference/footnote indexes. Fixture `fixtures/markdown/recursive-containers.md` + 5 tests prove nesting depth, containment, ordered top-level coverage, leaf-only inline ranges, and lazy continuation. Registered in the architecture parser registry. | typecheck; lint 0 errors; build exit 0; full Vitest 179 files / 2,391 passed + 1 skip; architecture guard 233 tests. | Self-acceptance: no renderer/editor/export regex scan discovers container children. | `codex/editor-foundation-refactor` |
| RF-403 | Physical line and prefix index | RF-402 | `COMPLETE` | Created the `@fishmark/editor-model` package (path alias + active architecture-guard rule) with `physical-lines/prefix-segment.ts` (ordered quote/indentation/list-marker/task-marker/spacing segments, tab-aware visible columns) and `physical-editing-document.ts` (lines from source + recursive tree, structural-blank/separator/fence/content roles, offset/line/node/visible-column queries). 4 tests cover nested quote+list+task prefixes, roles, queries, and tab geometry. Not yet consumed. | typecheck; lint 0 errors; build exit 0; full Vitest; architecture guard. | Self-acceptance: commands/decorations no longer need to reconstruct prefixes independently. | `codex/editor-foundation-refactor` |
| RF-404 | Incremental structure cache | RF-403 | `COMPLETE` | `packages/markdown-engine/src/parse/parse-checkpoint.ts` (safe checkpoints: depth 0 + no open fence), `cache/invalidation-range.ts` (`TextEdit`, `computeInvalidationWindow`, `applyTextEdit`), `cache/document-structure-cache.ts` (revision/source/tree), and `cache/incremental-document-parser.ts` (`applyIncrementalEdit` reuses the before-region by identity, reparses only the window slice, shifts the after-region by delta, and falls back to a fresh parse with `no-safe-window`). 8 tests: 5-case differential corpus vs. fresh parse, identity reuse, fallback, and no-edit. Cache is not yet consumed on any production path. | typecheck; lint 0 errors; build exit 0; full Vitest; architecture guard. | Self-acceptance: local edits invalidate only a bounded window, and a fresh parse remains the only correctness fallback. `perf:baseline` intentionally not rerun (cache has no production consumer or bundle impact yet). | `codex/editor-foundation-refactor` |
| RF-405 | Parser hard cutover | RF-404 | `COMPLETE` | `parseFullDocumentTree` is the only parser: shared leaf classifier (`parse/leaf-blocks.ts`), definition index (`parse/definition-index.ts`), indentation list scopes (`parse/list-scopes.ts`), and a rich-document projection of the tree (`parse/document-projection.ts`). `parse-block-map.ts` and the blockquote inner-source rescans are deleted; consumers use `parseMarkdownDocument`, the tree API, or the cache; container masking is a lazy `SourceText` view (20k-line fixture: tree 1.2s, rich document 2.2s, down from ~10s). A new guard test forbids `parseBlockMap`, `parseTopLevelBlocks`, and `createBlockquoteInnerSource`. | typecheck; lint 0 errors / 8 pre-existing warnings; build exit 0; full Vitest 181 files / 2,411 passed + 1 skipped; formal Electron behavior 121/121 cases and 2,541/2,541 targets with 0 unexpected/not-run; editing experience `pass: true`; architecture guard 234 tests. | Self-acceptance: one parser and one recursive tree remain, with no old parser export and no behavior regression in the formal corpus. | `codex/editor-foundation-refactor` |
| RF-501 | Semantic context and derived snapshot | RF-405 | `COMPLETE` | `EditorDerivedSnapshot` wraps one revision (tree, source, physical editing document) with line/node/path/table-cursor queries and an incremental-edit entry point; `deriveSelectionSnapshot` recomputes only the selection-derived active line/node/path/table cursor; `EditorSemanticContext` composes both and rejects stale revisions; `edit-transaction-plan.ts` defines ordered non-overlapping `TextEditOperation` plans and the command registry contract. 12 focused tests. | typecheck; lint 0 errors / 8 pre-existing warnings; build exit 0; full Vitest 183 files / 2,420 passed + 1 skipped; architecture guard 234 tests. | Self-acceptance: commands can read tree/line/path/selection with no parser call and no CodeMirror state. | `codex/editor-foundation-refactor` |
| RF-502 | Enter planner | RF-501 | `COMPLETE` | Pure `decideEnter`/`planEnter` over the semantic context: line container chain resolved by overlap, one rule per case (plain/structural-blank break with enclosing prefixes, heading exit, list continuation with ordinal/delimiter/task handling, one-level empty item and quote exits, fenced content lines, table-boundary row skeleton), each returning a single ordered `EditTransactionPlan` with the resulting caret. 14 focused tests cover the matrix through depth 8 and repeated Enter. | typecheck; lint 0 errors / 8 pre-existing warnings; build exit 0; full Vitest 184 files / 2,434 passed + 1 skipped. | Self-acceptance: no Enter behavior depends on DOM class names or feature-specific source rescans. | `codex/editor-foundation-refactor` |
| RF-503 | Backspace and Delete planners | RF-502 | `COMPLETE` | `decideBackspace`/`planBackspace` handle ordinary and range deletion, then degrade one hidden prefix per press (item marker → indentation, indentation run → outdent the whole subtree, quoted line → one quote level); `decideDelete`/`planDelete` delete forward and join the next line by removing its break plus prefix so the destination path is recomputed. Shared helpers extracted to `commands/line-structure.ts`; the physical-line→node mapping now prefers the child starting on the line. 12 focused tests. | typecheck; lint 0 errors / 8 pre-existing warnings; build exit 0; full Vitest 186 files / 2,448 passed + 1 skipped. | Self-acceptance: Backspace/Delete are source-minimal, subtree-safe, and depth-independent. | `codex/editor-foundation-refactor` |
| RF-504 | Indent, navigation, and selection policies | RF-503 | `COMPLETE` | `planIndentIn`/`planIndentOut` move the enclosing item subtree and anchor indentation after quote/parent prefixes; `navigation.ts` declares one policy per user intent (pointer, structural arrow, printable input, programmatic normalization), implements visible-line vertical navigation with a preferred visible column, a pointer plan without edits, a printable-input plan that only inserts text, and a normalization entry point that never changes structure. `selection-context.test.ts` covers normalization and ownership. 18 focused tests. | typecheck; lint 0 errors / 8 pre-existing warnings; build exit 0; full Vitest 189 files / 2,466 passed + 1 skipped. | Self-acceptance: selection policy is explicit and no global transaction filter guesses user intent. | `codex/editor-foundation-refactor` |
| RF-505 | Formatting, table, and fence planners | RF-504 | `COMPLETE` | Historical implementation/evidence is recorded in the 2026-08-14 RF-505 log below. | Historical 192 files / 2,491 passed + 1 skipped; not rerun by this status correction. | Historical self-acceptance; runtime cutover remains separately gated. | `codex/editor-foundation-refactor` |
| RF-506 | Semantic engine hard cutover | RF-HARDEN-001, RF-601 | `DEV_DONE` | Permanent production routing; old planners/context retired; Unicode and IME/data-frame review findings repaired; formal 121/121 and 2541 targets with zero unexpected/not-run. | Parent 2671 passed / 1 skip; lint/typecheck/build/editing PASS. Bundle FAIL retained for RF701/M6 combined acceptance. | Behavior/safety PASS; overall final performance pending. reports/task-summaries/RF-506.md | main |
| RF-601 | Transaction bridge, history, and IME | RF-505, RF-HARDEN-001 | `COMPLETE` | `@fishmark/codemirror-adapter` package: transaction conversion with monotonic cache revision and stale/foreign-session rejection, selection-snapshot reuse, pure composition freeze with one end-of-composition recompute, history grouping with isolated structural steps, `EditorChangeFramePort` onto the existing client, candidate performance probe. Focused 6 files / 56 tests; architecture guard 234 tests. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; full Vitest 200 files / 2,585 passed + 1 skipped; build exit 0. | Self-acceptance plus independent re-audit `PASS`; functional-runtime/IME acceptance stays with RF-506/RF-902. | `main` working tree, uncommitted |
| RF-602 | Viewport-scoped decorations | RF-506, RF-701 | `PLANNED` | — | adapter/layout/scroll tests/perf | — | — |
| RF-603 | Interaction adapters and widgets | RF-602 | `PLANNED` | — | adapter/visual tests | — | — |
| RF-604 | CodeMirror adapter hard cutover | RF-603 | `PLANNED` | — | lint/typecheck/test/build/probes | — | — |
| RF-701 | Semantic render plan | RF-506 | `PLANNED` | — | typecheck/test + production consumer | — | — |
| RF-702 | HTML export cutover | RF-604, RF-701 | `PLANNED` | — | test/build | — | — |
| RF-703 | Outline and metrics cutover | RF-702 | `PLANNED` | — | test/perf baseline | — | — |
| RF-801 | Non-React workspace client/store | RF-703 | `PLANNED` | — | typecheck/test | — | — |
| RF-802 | React shell decomposition | RF-801 | `PLANNED` | — | lint/typecheck/test | — | — |
| RF-803 | Main/preload composition split | RF-802 | `PLANNED` | — | lint/typecheck/test/build | — | — |
| RF-901 | Final performance gate | RF-803 | `PLANNED` | — | perf/editing probes | — | — |
| RF-902 | Playwright Electron data-safety/editing suite | RF-901 | `PLANNED` | — | build/e2e | — | — |
| RF-903 | Electron security hardening | RF-902 | `PLANNED` | — | test/build/e2e | — | — |
| RF-1001 | Dead code and compatibility purge | RF-903 | `PLANNED` | — | lint/typecheck/test/build/bundle | — | — |
| RF-1002 | Final architecture/behavior/docs acceptance | RF-1001 | `PLANNED` | — | all final gates | — | — |

## 4. Current accepted decisions

| Date | Decision | Consequence |
| --- | --- | --- |
| 2026-07-11 | Main-process `DocumentSession` is the sole writable business document truth. | Renderer sends revisioned edit commands and cannot maintain an independently writable workspace document snapshot. |
| 2026-07-11 | CodeMirror local input is represented as an ordered pending command queue. | Optimistic typing is allowed; save/switch/move/detach/reload/close require `flushEdits()`. |
| 2026-07-11 | Main text storage uses a domain `TextBuffer` port backed by CodeMirror `Text` in infrastructure. | Persistent text performance is reused without exposing CodeMirror types through domain contracts. |
| 2026-07-11 | Markdown containers use one recursive tree. | Lists, blockquotes, and their children share the same leaf semantics and source-range model. |
| 2026-07-11 | Incremental structure caching is a foundation requirement. | Selection changes do not parse; local edits invalidate bounded windows; fresh parse is the only correctness fallback. |
| 2026-07-11 | Editing experience has higher priority than syntax count and visual features. | Enter, Backspace, cursor, IME, undo, and round-trip gates can block every migration task. |
| 2026-07-11 | No permanent compatibility layer or dead code is allowed. | Every milestone ends with a hard cutover and deletion task. |
| 2026-07-11 | No parser worker is introduced in this program. | The synchronous incremental model must first meet measured budgets; another process requires a separate decision. |
| 2026-07-16 / amended 2026-07-28 | `@fishmark/workspace-domain` owns the single canonical workspace/session state; main owns its only live instance and maps immutable projections to shared workspace snapshot DTOs. File-backed ownership is the conjunction of canonical location and filesystem object identity, with ambiguous split ownership represented explicitly. | Draft mutation is a sender-derived owner compare-and-set; stale renderers neither mutate nor receive the target owner's projection. The coordinator owns tab/location/object lock ordering and issues opaque active capabilities for close-held IO. Duplicate open releases validation leases before requesting the owner renderer to flush and activate; main focuses only after exact confirmation and a fresh owner CAS. Reload revision staleness and stable identity/read/conflict errors are distinct no-projection results. Native close carries one request generation and its multi-tab capability through confirmation/save/drain. No direct snapshot-push event, service facade, reload allow-stale path, parameterless close compatibility path, duplicate close-save facade, dual state, or dead application Save API remains. |
| 2026-07-30 | `@fishmark/workspace-application` is the single runtime-neutral owner of current workspace workflow orchestration and exposes only consumed ports through one public entry. | Main constructs native adapters and maps IPC DTOs only. Typed success/cancel/stale/conflict/error results cross explicit exhaustive mappings; application code cannot import Electron, React, DOM, Node, shared IPC DTOs, CodeMirror, or future infrastructure. Superseded main-local business modules and compatibility aliases are deleted. |
| 2026-08-04 | Domain/application remain runtime-neutral through `TextBuffer`/`TextBufferFactory`; private workspace infrastructure owns CodeMirror `Text`, and production main injects its factory explicitly. Revisioned sessions keep one contiguous acknowledged sequence high-watermark per client. | Persistent edits reuse structural sharing without CodeMirror type leakage or dual truth. The existing full-draft transport remains only until RF-204 and is not a second canonical owner; infrastructure dependency and resolved source paths fail closed through the package allowlist, public entry, and source-root containment. |
| 2026-08-04 | RF-202 defines one staged, structured-clone-safe revisioned edit transport: shared owns wire DTOs, application/domain own runtime-neutral validation and sequencing, main derives authorization from the live sender and rechecks it inside the same per-tab critical section, and preload has one complete `ProductBridge` builder guarded by canonical-symbol/runtime-owner analysis. | Applied/duplicate acknowledgements and projection events contain metadata only; canonical Markdown crosses only for explicit revision-conflict recovery. Apply and `throughSequence` flush serialize together. RF-203 may now build the renderer queue; RF-204 must delete the intentionally retained full-draft channel without introducing a compatibility route or second owner. |

| 2026-08-14 | M4 and M5 (RF-401 through RF-505) are merged into `main`; RF-506 remains the last M5 task. | The merge lands the single recursive parser, the incremental cache, the rich-document projection, and the complete pure semantic engine (context, Enter, Backspace/Delete, indent/navigation, formatting/table/code-fence) with all gates and the formal Electron corpus green. RF-506 is deferred because it deletes ~8.2k lines of old semantic code and requires a CodeMirror-to-editor-model bridge plus rewiring of keyboard, menu, toolbar, table widget, and test-driver commands; doing it together with this merge would leave the editor non-functional. |

| 2026-09-18 | The CodeMirror conversion layer lives in `@fishmark/codemirror-adapter`, not in `editor-core`. | The guard forbids `@codemirror/*` imports outside the adapter, so the bridge state field moved with the conversion layer instead of keeping two implementations or a new perpetual exception. The adapter owns the per-view structure cache, selection mapping, composition state, and history grouping; `@fishmark/editor-model` keeps every Markdown rule; `src/renderer/application/{pending-edit-queue,workspace-edit-client}.ts` stay the only queue and transport owners, reached through a narrow frame port. RF-604 still owns switching the renderer editor factory to the adapter. |

| 2026-09-18 | A plan's origin session must be recorded, not assumed. | `EditorPlanSessions` records the generation that produced each plan, so a plan from a superseded binding is rejected as `foreign-session`. Comparing a plan against the current session it was just read from is a tautology and cannot detect a rebind. |

## 5. Current repair handoff

`RF-HARDEN-001` is `COMPLETE` with parent acceptance `PASS`. Intake and handoff are in `docs/plans/2026-09-17-rf-harden-001-{intake,handoff}.md`; fresh evidence and manual steps are in `reports/task-summaries/RF-HARDEN-001.md`. Full Vitest: 195 files, 2,535 passed / 1 skipped; independent audit: 19/19; build/typecheck/lint and formal runtime behavior passed. RF-601 is next, RF-506 remains planned. No general incremental latency, platform IME or whole-program completion is claimed.

## 5H. Historical RF-202 accepted task handoff

The following text describes its 2026-08-04 state only; the current task and dependency order are above.

### Task

`RF-202 — Shared edit contract and main handler`

### Status

`COMPLETE` — independent architecture review and formal task acceptance passed on 2026-08-04. M2 is 2/4 `IN_PROGRESS`, program completion is 6/38, and there is no active task.

### Goal and accepted result

Revisioned edit batches now cross one structured-clone-safe preload/main boundary into the runtime-neutral application/domain path. Main derives owner authority from the live sender and rechecks it inside the same per-tab coordinator used by apply and `throughSequence` flush, so a queued stale renderer cannot mutate or read a foreign checkpoint. Normal acknowledgements and projection events remain metadata-only; only explicit revision conflict returns canonical Markdown. The current full-draft channel remains exactly as RF-204 deletion debt, not as a second canonical owner or feature-routed compatibility structure.

### Verification

- Fresh RF-202 focus: 24 files / 529 tests; editor-foundation: 7 files / 307 tests.
- Lint passed with 0 errors / 8 existing warnings; typecheck passed.
- Full Vitest: 172 files / 2,232 passed + 1 explicit skip out of 2,233 total.
- Build passed for renderer, Electron, CLI, and all three workspace runtime verifiers; the existing large-chunk advisory remains.
- Formal editor behavior: 121/121 cases, 2,541/2,541 targets, 79 existing, 1,928 runner, 534 known defects, 0 unexpected, 0 not-run in 25,869 ms.
- Independent architecture acceptance: `PASS`, P0/P1/P2 = 0, no open questions. Final quality review: Critical 0, Important 0, Minor 1, `Ready: Yes`; the remaining direct parameterized-test note is non-blocking. Formal task acceptance: `PASS`.
- `git diff --check`: exit 0; line-ending warnings only.

### Documentation and next state

- Formal record and manual steps: `reports/task-summaries/RF-202.md`.
- Intake and execution handoff: `docs/plans/2026-08-04-rf-202-intake.md` and `docs/plans/2026-08-04-rf-202-handoff.md`.
- RF-202 is the latest accepted task. RF-203 is dependency-ready `PLANNED` and has not started; starting it requires a fresh one-time intake.

## 5A. Historical accepted task handoff

### Task

`RF-002 — Architecture and performance guards`

### Status

`COMPLETE` — independent architecture acceptance and formal task acceptance passed on 2026-07-15. This RF-002 handoff is retained as historical context; the current latest accepted task is RF-201 above.

### Goal

Freeze current dependency/parser lifecycle, one canonical 20,000-line RF fixture, honest operation counters, and deterministic emitted-bundle evidence before moving editor foundations.

### In scope

- One fail-closed architecture manifest for active/planned packages, parser lifecycle, exact debt exceptions, retirements, and public bundle checks.
- Registered public/internal document parser surfaces and supported direct micromark document-site forms.
- One committed, hash-bound 20,000-line Markdown fixture.
- Real current open/edit/selection/ordered-list/outline/metrics counters with explicit unavailable capability reasons.
- Schema-v1 Vite provenance and strict emitted Source Map validation, with `moduleIds` as forbidden source-group authority.

### Out of scope

- Recursive parser/cache implementation, parser worker, semantic command refactor, CodeMirror adapter extraction, workspace ownership migration, and persistence changes.
- Millisecond CI thresholds or claims of incremental reuse that does not exist yet.
- User-visible syntax, formatting, selection, IME, undo, autosave, or save behavior changes.

### Landing area

- `fixtures/architecture/editor-foundation-guard.json`
- `fixtures/performance/complex-20000-lines.md`
- `fixtures/performance/editor-foundation-current-baseline.json`
- `src/main/editor-foundation-architecture*.ts`
- `packages/markdown-engine/src/parse-instrumentation.ts`
- `packages/editor-core/src/performance/`
- `src/renderer/performance/`
- `scripts/analyze-renderer-bundle.mjs`
- `scripts/bundle-provenance-evidence.mjs`
- `scripts/vite-bundle-provenance.ts`

### Acceptance

- The canonical manifest is versioned, non-empty, exact, lifecycle-owned, and fail-closed for invalid paths/rules, stale debt, boundary bypasses, and unauthorized parser surfaces/sites.
- The committed RF fixture is exactly 20,000 LF-only lines and matches SHA-256 `545601ad9d770898e23e5551e938bcaa4ebaecd8446941a4145a4db8777f6d37` before measurement.
- Every operation exposes deterministic integer counters. Unavailable incremental behavior remains zero with `incremental-structure-cache-not-implemented`.
- Bundle JSON/provenance are deterministic, schema-versioned, hash-bound, graph-authoritative, and explicit about emitted maps versus `NOT_EMITTED`.
- No compatibility identity-map/helper or dead subordinate formatter remains in RF-002 scope.
- Product behavior remains unchanged under the formal Electron gate.

### Verification

```powershell
npm.cmd run test:editor-foundation
npm.cmd run perf:baseline
npm.cmd run lint
npm.cmd run typecheck
npm.cmd test
npm.cmd run build
npm.cmd run test:editor-behavior
git diff --check
```

### Risks

- Current full scans are intentionally high and incremental reuse remains unavailable until RF-404/RF-405.
- The 73 exact CodeMirror edges remain RF-604 deletion debt.
- Timing remains machine-dependent until RF-901.
- The 534 known editing defects remain later-roadmap work.
- Focus-derived geometry observation settlement should be hardened separately without changing calibration or editor behavior.

### Documentation updates

- Architecture decision and `TC-060A` are recorded.
- Fresh evidence is in `docs/test-report.md`.
- Formal result and manual steps are in `reports/task-summaries/RF-002.md`.
- The independent RF roadmap/progress ledger is complete for RF-002; unrelated MVP backlog/progress were intentionally not changed.

### Next skill

`$fishmark-architecture-acceptance` for the `RF-101` implementation range, followed by `$fishmark-task-acceptance` only if the architecture review passes.

## 6. Milestone deletion checklist

A milestone cannot become `COMPLETE` until its row below is checked.

- [x] M1: `src/main/workspace-service.ts`, main-local workspace application/coordinator, and obsolete tests are deleted.
- [ ] M2: full-draft update IPC, renderer local content projection mutation, snapshot-preservation logic, and old mocks are deleted.
- [ ] M3: active-tab-only watcher and renderer-owned external conflict controller/state are deleted.
- [ ] M4: old block-map parser, rich-parser stitching, container rescans, and duplicate inline parsing paths are deleted.
- [ ] M5: old semantic commands, physical-line/prefix parsers, and semantic context implementations are deleted.
- [ ] M6: `packages/editor-core` and its aliases/imports are deleted after valid code is moved.
- [ ] M7: export/outline/metrics parser duplication and duplicate semantic render helpers are deleted.
- [ ] M8: direct privileged bridge calls from components and workflow effects in React/main entry files are deleted.
- [ ] M9: temporary performance logging, unrestricted asset paths, unsandboxed renderer configuration, and missing E2E placeholders are deleted.
- [ ] M10: every remaining compatibility symbol, unused dependency, dead export, stale test, stale probe, and contradictory document is deleted or corrected.

## 7. Evidence log

Append one entry when a task changes to `DEV_DONE`, then amend the same entry after acceptance. Do not add entries for planning-only discussion.

| Date | Task | Change summary | Focused verification | Full gates | Acceptance | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-07-13 | RF-001 | Added the first typed recursive editing-behavior corpus, exact filtering, one harness scenario, and stable documentation; corrected one stale table-exit probe expectation without changing runtime behavior. | Matrix 1/13; harness + driver 14/81; defect evidence 2/78. | Editing experience 79 cases passed; lint, typecheck, 119-file/1380-test suite, and build passed. | Superseded before acceptance | A 2026-07-14 specification review found that metadata-only parity cases had been overstated as executable evidence. RF-001 returned to `IN_PROGRESS`; this row is historical regression evidence only. |
| 2026-07-14 | RF-001 | Added the single-window Electron manifest runner and hardened it with canonical/formal support ports, line-local observation, aspect-mapped values, deterministic calibration identity, catalog-driven Typora conversion, calibration-independent raw cases, disposable step abort listeners, and bounded cross-platform process-tree ownership; corrected top-level and quoted nested-list Shift+Tab contracts without changing product runtime behavior. | Fresh harness + driver 27 files / 165 tests; public scenario 121/121 one batch; architecture review also confirmed formal identity, listener, timeout/abort/nonzero/root-exited descendant, hung Windows terminator, POSIX group escalation, test-window security, and no compatibility/dead path. | Fresh formal runner 121/121 and 2,541/2,541 in 24.573s with 79 existing/1,928 runner/534 known/0 unexpected/0 not-run; legacy 79/79; lint 0 errors/8 existing warnings; typecheck; 135-file/1,487-test suite; build; diff check all passed. | Architecture `PASS`; task `PASS`; `reports/task-summaries/RF-001.md` | Raw JSON remains ignored; calibration `fnv1a32-2a007600` persists 2,007 explicit verified targets and 534 exact typed defect observations from run `d7705d03-b141-46dd-8b4b-4559024c14bc`. The 534 observations are later-roadmap product work, not unexecuted RF-001 targets. |
| 2026-07-15 | RF-002 | Final architecture closure added transparent-wrapper/computed-property micromark detection, transparent literal module-argument and literal `require` callee evidence, reverse-unique active package rules and resolved/case-folded repository path identities; replaced inferred parse-entry counts with explicit micromark document-scan instrumentation; deleted subordinate formatters; and replaced Source Map text-coverage/identity-map assumptions with schema-v1 two-phase Vite provenance. Provenance `moduleIds` are the only forbidden source-group authority; emitted maps are integrity validated and attested mapless chunks are `NOT_EMITTED`. | Initial architecture RED 145/131 pass + 14 expected failures -> GREEN 145/145. Post-`3e0c34e` RED 164/146 pass + 18 expected failures -> GREEN 164/164. Post-`3d8c2b4` RED 177/166 pass + 11 expected failures -> GREEN 177/177. Fresh public foundation 7 files/253 tests; fresh `perf:baseline` passed all 23 canonical checks and 7 files/253 tests. Exact full scans are open 504, edit 502, selection 0, ordered-list 503, outline 502, metrics 503. | Fresh lint 0 errors/8 pre-existing warnings; typecheck; 138 files/1,738 tests; renderer/Electron/CLI build; exclusive formal behavior 121/121 cases and 2,541/2,541 targets with 79 existing/1,928 runner/534 known/0 unexpected/0 not-run in 24.570s; ordinary dist 0 maps/0 provenance; diff check PASS. | Architecture `PASS` over `a6da237..53824a7`, 0 blocking findings/no open questions; task `PASS`; `reports/task-summaries/RF-002.md` | M0 is 2/2 `COMPLETE` and program is 2/38. Final closure commits: `3e0c34e`, `3d8c2b4`, `53824a7`. Earlier execution-phase focus-derived visibility-only flakes were isolated and followed by clean repeats; the first exclusive formal acceptance run was clean. No calibration/runtime change was made. |
| 2026-07-16 / amended 2026-07-28 | RF-101 | Added the production runtime-neutral workspace-domain package and hard-cut main from the old service. Post-review closure adds exact CodeMirror-acknowledged sealing/sealed/releasing transitions, structural hard read-only, fresh-state canonical undo boundaries, recoverable epoch-only rebind, a bounded reload ambiguity checkpoint, admission and post-await disposal fences, and a typed application-owned editor test adapter. The formal P1 closure deletes shell-state draft merge/reload compatibility and its dead tab getter so only the renderer application owns reconciliation. | Earlier RED covered transition/history/reload/disposal gaps. Formal-review RED then proved obsolete shell-state ownership tokens remained; GREEN makes snapshot application canonical-only and guards against reintroduction. Latest related focus is 4 files / 391 tests; renderer focus is 13 files / 677 tests. | Fresh typecheck, lint (0 errors / 8 existing warnings), and build passed. The preceding escalated full Vitest evidence is 163 files / 2,052 passed plus 1 explicit skip out of 2,053 total. | First formal architecture run failed on the now-fixed P1; architecture re-review and task acceptance remain pending, with no verdict recorded by execution. | M1 remains 0/2 complete and program remains 2/38 until acceptance. `RF-102` remains blocked on RF-101 `COMPLETE`; implementation closure is recorded in the task handoff. |
| 2026-07-28 | RF-101 | Formal acceptance verified the complete workspace-domain, main transaction, renderer transaction, transition barrier, deletion, and documentation scope without changing implementation code. | Fresh renderer/editing focus 13 files / 863 tests; roadmap workspace-domain/main focus 16 files / 217 tests; editor-foundation 7 files / 260 tests. | Fresh escalated full Vitest 163 files / 2,052 passed + 1 skip; typecheck, lint 0 errors / 8 existing warnings, build, and final exclusive editor behavior 121/121 cases / 2,541/2,541 targets / 0 unexpected / 0 not-run passed. Residue and diff checks passed. | Independent architecture `PASS` over `f5aa70b..003d4d5`, P0/P1/P2 = 0, no open questions; formal task acceptance `PASS`; `reports/task-summaries/RF-101.md`. | At that acceptance checkpoint, M1 was 1/2 `IN_PROGRESS` and program completion was 3/38; current state is recorded by the later RF-102 and RF-201 rows. |
| 2026-07-30 | RF-102 | Added the runtime-neutral workspace application package and hard-cut all RF-101 main-local workspace business implementations. The semantic workflow modules and complete façade own mutation-to-watcher order, typed stale/conflict/error outcomes, close Save/Save As routing, and exhaustive native-close result mapping; main owns native composition/adapters and IPC mapping only. | Focused application/main/integration/identity/architecture gate 23 files / 561 tests; editor-foundation 7 files / 267 tests. | Fresh lint 0 errors / 8 existing warnings; typecheck; full Vitest 165 files / 2,066 passed + 1 skip; build including both workspace runtime verifiers; formal behavior 121/121 cases and 2,541/2,541 targets with 0 unexpected/not-run; forbidden/residue scans and diff check passed. | Architecture `PASS`, P0/P1/P2 = 0; final quality Critical/Important/Minor = 0, `Ready: Yes`; task `PASS`; `reports/task-summaries/RF-102.md`. | RF-102 and M1 are `COMPLETE`; program reached 4/38 at that acceptance checkpoint. Current state is recorded by the later RF-201 row. |
| 2026-08-04 | RF-201 | Added the persistent CodeMirror `Text` production buffer behind the runtime-neutral domain port, explicit session/workspace factories, revisioned idempotent edit batches, one contiguous high-watermark per client, runtime-malformed change validation, runtime build verification, and an active fail-closed infrastructure boundary with public-entry and source-root containment; execution handoff: `docs/plans/2026-08-04-rf-201-handoff.md`. | Earlier domain/infrastructure/application/main focus: 63 files / 816 passed + 1 existing skip; post-fix domain/infrastructure 4 files / 163 tests and architecture 1 file / 209 tests; editor-foundation 7 files / 285 tests. | Lint passed with 0 errors / 8 existing warnings; typecheck passed; full Vitest passed 166 files / 2,162 tests + 1 skip; renderer/Electron/CLI build passed with all three workspace runtime verifiers; formal behavior passed 121/121 cases and 2,541/2,541 targets with 0 unexpected/not-run; final acceptance-document `git diff --check` exited 0. | Specification/architecture `PASS`, P0/P1/P2 = 0 and no open questions after the progress-doc P1 correction; final quality Critical/Important/Minor = 0 and `Ready: Yes` after malformed-insert and relative-import RED-to-GREEN fixes; task `PASS`; `reports/task-summaries/RF-201.md`. | At the RF-201 acceptance checkpoint, M2 was 1/4 and program completion was 5/38; current state is recorded by the later RF-202 row. |
| 2026-08-04 | RF-202 | Added the shared revisioned edit transport contract and metadata projection, owner-aware domain/application apply and through-sequence flush barrier, mandatory sender authorization inside the same per-tab critical section before domain access, truthful staged wire candidates, explicit IPC result reconstruction, and a TypeScript Program/TypeChecker-backed preload builder invariant rooted at the canonical shared symbol. The invariant normalizes target evidence to runtime construction owners and safely resolves one canonical returned local-const chain before checking top-level spread. Normal acknowledgements/events are content-free; conflict alone carries canonical text. The legacy full-draft channel remains under `updateDocumentDraft` for RF-204. | RED/GREEN is recorded in `docs/plans/2026-08-04-rf-202-handoff.md`; fresh acceptance focus 24 files / 529 tests; ProductBridge architecture focus 1 file / 231 tests; real cross-layer, stale-sender no-mutation/no-checkpoint, queued-owner-race, malformed-duplicate, symbol-resolution, runtime-construction dedupe, returned-local spread, ambiguous-return, and wire-field-isolation coverage included. | Editor-foundation 7 files / 307 tests; lint 0 errors / 8 existing warnings; typecheck; full Vitest 172 files / 2,232 passed + 1 skip out of 2,233; renderer/Electron/CLI build with all three workspace runtime verifiers; formal behavior 121/121 cases / 2,541/2,541 targets / 0 unexpected / 0 not-run in 25,869 ms; diff check passed with line-ending warnings only. | Independent architecture `PASS`, P0/P1/P2 = 0, no open questions; final quality Critical 0, Important 0, Minor 1, `Ready: Yes`; formal task acceptance `PASS`; `reports/task-summaries/RF-202.md`. | RF-202 is `COMPLETE`; M2 is 2/4 `IN_PROGRESS`; program completion is 6/38. RF-203 is dependency-ready `PLANNED` and not started. The one quality-review Minor is direct parameterized-test coverage for non-local/cyclic/reassigned fail-closed branches and is non-blocking. |

| 2026-08-13 | RF-203 | Added the pure runtime-neutral renderer pending edit queue and workspace edit client, a single RAF frame bucket with composition-aware seal, the internal-origin remote patch boundary, and a production hard-switch to revisioned edit frames with the legacy full-draft channel kept only as dormant RF-204 debt. Non-conflict recovery sources (adapter discard, invalid frame, missing sequence) now auto-materialize a recovery tab through the same coordinator as the conflict path, closing the quality-review Critical where getRecoveryPendingOutcome could permanently block window close. Independent spec review: 9/9 Acceptance PASS; quality review Ready:No initially with Critical C1 fixed and regression tests added; Important/Minor items recorded as RF-204-deferred robustness/cleanup. | Focused 6 files / 636 tests (queue 51, client 51, editor adapter, application, autosave, controller); editor-foundation 7 files / 308 tests; architecture guard 232 tests. | Lint 0 errors / 8 existing warnings; typecheck; full Vitest 174 files / 2,413 passed + 1 skip; build exit 0 with all three workspace runtime verifiers; formal behavior 121/121 cases / 2,541/2,541 targets / 0 unexpected / 0 not-run on exclusive rerun (earlier runs showed the known physical-geometry/focus settling flake). | Independent spec review 9/9 PASS; quality review Critical C1 fixed; architecture PASS, P0/P1/P2 = 0; formal task acceptance PASS; reports/task-summaries/RF-203.md. | RF-203 is COMPLETE; M2 is 3/4 IN_PROGRESS; program completion is 7/38. RF-204 is the next dependency-ready task. |
| 2026-08-14 | RF-301 | Replaced the active-tab-only `external-file-watch-service` with a per-path watch registry (`src/main/infrastructure/file-watch-registry.ts`) that keys entries by normalized path with a per-window subscriber set, syncs every open tab path through the new `WorkspaceWatcherPort.syncWindowPaths(context, tabPaths[])`, suppresses the app's own writes via `beginInternalWrite`/`completeInternalWrite`, and closes a shared watcher only after its last subscriber unsubscribes or is destroyed. Deleted `external-file-watch-service.ts` and its 714-line test; migrated application ports/use cases, main wiring, and all `syncDocumentPath` mocks. | Focused registry 8 tests + workspace-application 19 + touched main integration 58 tests. | lint 0 errors / 8 pre-existing warnings; typecheck; full Vitest 172 files / 2,358 passed + 1 skip; build exit 0. | Self-acceptance: exit criterion verified (inactive-tab edits detected and delivered, own writes suppressed, shared watchers torn down). | RF-301 is COMPLETE; M3 is 1/4 IN_PROGRESS; program completion is 9/38. RF-302 is the next dependency-ready task. |
| 2026-08-14 | RF-302 | Added `src/main/infrastructure/document-repository.ts` (read+hash `readDiskVersion`, safe temp-write + atomic rename `writeDocument`) and the `DiskRepositoryPort` application port; `save-document.ts` now rejects a normal save with `disk-version-conflict` when the on-disk `contentHash` diverges from the session version and commits the post-write `DiskVersion`; `DiskVersion` is recorded at open (`openDocument` third arg), reload (`replaceTabDocument`), and save; the read adapter now returns `diskVersion`; `saveMarkdownFileToPath` deleted. Deleted three obsolete adapter-echo tests and added one `disk-version-conflict` rejection test. | Focused repository 5 + save/open/reload/document-io/file-identity-races/window-close suites (227 tests). | lint 0 errors / 8 pre-existing warnings; typecheck; full Vitest 173 files / 2,354 passed + 1 skip; build exit 0. | Self-acceptance: exit criterion verified (stale disk content is rejected before overwrite; atomic replace; disk version threaded end to end). | RF-302 is COMPLETE; M3 is 2/4 IN_PROGRESS; program completion is 10/38. RF-303 is the next dependency-ready task. |
| 2026-08-14 | RF-303 | Added the main-owned recovery stack: checksummed `recovery-journal.ts`, `workspace-persistence.ts`, application `recovery.ts`, and `recovery-service.ts`, plus domain `exportSnapshot`/`restoreSnapshot`; `main.ts` journals every accepted edit, restores snapshot + replays the journal on startup, compacts on `before-quit`, and quarantines corrupt files. | Focused recovery/journal/persistence suites plus full Vitest 173 files / 2,354 passed + 1 skip. | lint 0 errors / 8 pre-existing warnings; typecheck; build exit 0. | Self-acceptance: recovery state is main-owned and renderer-memory-free. | RF-303 is COMPLETE; M3 is 3/4 IN_PROGRESS; program completion is 11/38. |
| 2026-08-14 | RF-304 | Made conflict and close workflows main-owned: session-level `externalChange` + `markExternalChange`, watch-driven marking, projection of the conflict, application `resolve-external-change.ts` typed commands (keep-memory/reload/save-as/cancel), `resolveExternalChange` IPC + preload bridge, renderer conflict derivation only, and deletion of `useExternalConflictController.ts` (+ test) and the shell-state conflict reducers. Autosave is blocked by the projected conflict. | Focused application/editor-shell/main suites; full Vitest 176 files / 2,377 passed + 1 skip. | lint 0 errors / 8 pre-existing warnings; typecheck; build exit 0. | Self-acceptance: conflict resolution and close are main-owned and projected; no renderer-owned conflict state remains. | RF-304 is COMPLETE; M3 is COMPLETE; program completion is 12/38. |
| 2026-08-14 | RF-401 | Added the parser-agnostic recursive model under `packages/markdown-engine/src/model/`: `source-range.ts`, `container-path.ts`, `markdown-node.ts`, and `document-tree.ts` with FNV-1a ancestry+subtree identity, tree indexing, navigation, and invariants; 9 focused tests cover CRLF/tabs/Unicode/empty containers/lazy continuation/depth 0–8. The legacy block-map parser is untouched. | Focused model suite 9 tests; full Vitest. | typecheck; lint 0 errors; build exit 0. | Self-acceptance: the model expresses every recursive parity path without top-level special cases. | RF-401 is COMPLETE; M4 is 1/5 IN_PROGRESS; program completion is 13/38. |
| 2026-08-14 | RF-402 | Added `parse/micromark-event-adapter.ts` and `parse/full-document-parser.ts`, driving one container stack (`blockQuote`/`listOrdered`/`listUnordered` plus synthetic `listItemPrefix` item frames) into the recursive model, unioning lazy-continuation ranges and masking container prefixes before leaf inline parsing; fixture `fixtures/markdown/recursive-containers.md` + 5 tests prove nesting depth, containment, ordered top-level coverage, leaf-only inline ranges, and lazy continuation. Registered in the architecture parser registry. | Focused parser suite 5 tests; full Vitest 179 files / 2,391 passed + 1 skip; architecture guard 233 tests. | typecheck; lint 0 errors; build exit 0. | Self-acceptance: no renderer/editor/export regex scan discovers container children. | RF-402 is COMPLETE; M4 is 2/5 IN_PROGRESS; program completion is 14/38. |
| 2026-08-14 | RF-403 | Created the `@fishmark/editor-model` package (path alias + active architecture-guard rule) with `physical-lines/prefix-segment.ts` (ordered quote/indentation/list-marker/task-marker/spacing segments, tab-aware visible columns) and `physical-editing-document.ts` (lines derived from source + recursive tree, structural-blank/separator/fence/content roles, offset/line/node/visible-column queries). 4 tests cover nested quote+list+task prefixes, roles, queries, and tab geometry. Not yet consumed. | Focused editor-model suite 4 tests; full Vitest; architecture guard. | typecheck; lint 0 errors; build exit 0. | Self-acceptance: commands/decorations no longer reconstruct prefixes independently. | RF-403 is COMPLETE; M4 is 3/5 IN_PROGRESS; program completion is 15/38. |
| 2026-08-14 | RF-404 | Added `parse/parse-checkpoint.ts`, `cache/invalidation-range.ts`, `cache/document-structure-cache.ts`, and `cache/incremental-document-parser.ts`: safe checkpoints (depth 0, no open fence), bounded invalidation windows, identity-reuse of the before-region, window-local reparse with delta shift of the after-region, and a fresh-parse `no-safe-window` fallback. 8 tests include a 5-case differential corpus against a fresh parse. The cache has no production consumer yet. | Focused cache suite 8 tests; full Vitest. | typecheck; lint 0 errors; build exit 0. | Self-acceptance: local edits invalidate only a bounded window with a fresh parse as the only correctness fallback. | RF-404 is COMPLETE; M4 is 4/5 IN_PROGRESS; program completion is 16/38. RF-405 is the next dependency-ready task. |

| 2026-08-14 | RF-405 | Made the recursive tree the single parser. Leaf classification moved to `parse/leaf-blocks.ts`, reference/footnote indexes to `parse/definition-index.ts`, indentation list scoping to `parse/list-scopes.ts`, and the rich document became a projection of the tree (`parse/document-projection.ts`) with legacy offsets, markers, and camelCase block ids. Deleted `parse-block-map.ts` together with the blockquote inner-source rescan/normalization machinery and its obsolete lean-parser tests; migrated `active-block`, `list-edits`, `extensions/markdown`, the performance probe, renderer probes, and their tests to `parseMarkdownDocument`; container masking became a lazy `SourceText` view. | Full Vitest 181 files / 2,411 passed + 1 skipped; architecture guard 234 tests; 54-test block-structure corpus unchanged. | typecheck; lint 0 errors / 8 pre-existing warnings; build exit 0; formal Electron behavior 121/121 cases and 2,541/2,541 targets (0 unexpected, 0 not-run, 24.6s); editing experience `pass: true`; 20k-line fixture 1.2s tree / 2.2s document. | Self-acceptance: one parser and one recursive tree remain; the guard fails closed if the retired parser symbols or the deleted `parse-block-map.ts` return. | RF-405 and M4 are COMPLETE; program completion is 17/38. RF-501 is the next dependency-ready task. |

| 2026-08-14 | RF-501 | Added the pure semantic context layer to `@fishmark/editor-model`: document-derived `EditorDerivedSnapshot`, selection-derived `EditorSelectionSnapshot`, `EditorSemanticContext` with stale-revision rejection and selection-only recompute, and the `EditTransactionPlan`/command registry contract. Exported the model `MarkdownTableCell`/`MarkdownTableRow` types from the engine so the cursor type stays canonical. | Focused `packages/editor-model` 12 tests; architecture guard 234 tests. | typecheck; lint 0 errors / 8 pre-existing warnings; build exit 0; full Vitest 183 files / 2,420 passed + 1 skipped. | Self-acceptance: the context is immutable, parser-free, CodeMirror-free, and rejects stale revisions. | RF-501 is COMPLETE; M5 is 1/6 IN_PROGRESS; program completion is 18/38. RF-502 is the next dependency-ready task. |

| 2026-08-14 | RF-502 | Added the pure Enter planner to `@fishmark/editor-model`: `decideEnter` reports the rule it applied while `planEnter` returns the single ordered `EditTransactionPlan`. Lines resolve their container chain by overlap, so empty container lines and nested prefixes behave at every depth; ordered items advance ordinal/delimiter, task items restart unchecked, an empty item or quote line exits exactly one level, fenced lines repeat their prefixes, and a table boundary adds an empty row. Also taught `EditorDerivedSnapshot.nodeAt` that a caret at a node end belongs to that node. | Focused `packages/editor-model` 26 tests (12 RF-501 + 14 RF-502). | typecheck; lint 0 errors / 8 pre-existing warnings; build exit 0; full Vitest 184 files / 2,434 passed + 1 skipped. | Self-acceptance: Enter is semantic-context-only, prefix-preserving, single-transaction, and covered through depth 8. | RF-502 is COMPLETE; M5 is 2/6 IN_PROGRESS; program completion is 19/38. RF-503 is the next dependency-ready task. |

| 2026-08-14 | RF-503 | Added the Backspace and Delete planners to `@fishmark/editor-model` plus the shared `commands/line-structure.ts` helpers. Backspace deletes a character or range and then degrades exactly one hidden prefix (marker, indentation with the whole subtree, or one quote level); Delete mirrors it forward and joins the next line by removing its break and prefix. Fixed the physical-line→node mapping so a line belongs to the child starting on it rather than the previous sibling whose range still covers its trailing break. | Focused `packages/editor-model` 40 tests (12 RF-501, 14 RF-502, 7 RF-503 backspace, 7 RF-503 delete). | typecheck; lint 0 errors / 8 pre-existing warnings; build exit 0; full Vitest 186 files / 2,448 passed + 1 skipped. | Self-acceptance: deletion degrades one step, preserves subtrees, and recomputes the destination path. | RF-503 is COMPLETE; M5 is 3/6 IN_PROGRESS; program completion is 20/38. RF-504 is the next dependency-ready task. |

| 2026-08-14 | RF-504 | Added the indent/outdent planners and explicit navigation policies to `@fishmark/editor-model`. Indentation lands after quote and parent prefixes so quoted lists stay valid, and outdenting moves every covered line of the subtree. `navigation.ts` splits pointer, structural arrow, printable input, and programmatic normalization into declared policies, implements visible-line vertical navigation with a preferred visible column, and proves printable input never changes structure. Added the missing `selection-context.test.ts`. | Focused `packages/editor-model` 58 tests. | typecheck; lint 0 errors / 8 pre-existing warnings; build exit 0; full Vitest 189 files / 2,466 passed + 1 skipped. | Self-acceptance: Tab/Shift+Tab/arrows/pointer/input policies are explicit and non-conflicting. | RF-504 is COMPLETE; M5 is 4/6 IN_PROGRESS; program completion is 21/38. RF-505 is the next dependency-ready task. |

| 2026-08-14 | RF-505 | Added the formatting, table, and code-fence planners to `@fishmark/editor-model`. Toggles rewrite only the exact marker ranges and read line patterns after quote prefixes, so quoted and nested content keeps its structure. Table operations run against the canonical table node (cell navigation, row/column insert and delete, table delete, cell update, boundary exit) and rebuild the canonical spelling through the engine formatter. Code fences cover wrap/unwrap, language completion, content Enter, closing boundary exit, and content indentation. | Focused `packages/editor-model` 83 tests. | typecheck; lint 0 errors / 8 pre-existing warnings; build exit 0; full Vitest 192 files / 2,491 passed + 1 skipped (one load-related release-build flake passed on rerun). | Self-acceptance: all remaining semantic edits share the plan/context boundary with no CodeMirror imports. | RF-505 is COMPLETE; M5 is 5/6 IN_PROGRESS; program completion is 22/38. RF-506 is the next dependency-ready task. |

| 2026-08-14 | RF-506 (preparation) | Ported the last missing pure semantics before the cutover: `commands/list-move.ts` (move an item subtree up/down through canonical list scopes, with ordered renumbering and the loose-item restart rule) and `commands/ordered-list.ts` (ordered scope normalization). Exposed `readListScopes`, `readFlatListItems`, `collectBlockquotePrefixSpans` and their types from `@fishmark/markdown-engine` so commands and the parser share one list-scope reader. Also corrected the loose-item tail rule so indented continuation lines do not restart numbering. | Focused `packages/editor-model` 93 tests (10 new). | typecheck; lint 0 errors / 8 pre-existing warnings; full Vitest 193 files / 2,501 passed + 1 skipped (one load-related renderer flake passed on rerun). | Preparation only: the RF-506 consumer cutover (CodeMirror bridge, command routing, ~8.2k-line deletion) is still pending and the branch is otherwise identical to `main`. | RF-506 remains PLANNED; M5 is 5/6. |

| 2026-08-14 | RF-506 (preparation) | Added the editor-model command bridge (`packages/editor-core/src/commands/editor-model-bridge.ts`): a per-view `DocumentStructureCache` state field that updates incrementally for single-range edits and reparses for multi-range changes, plus `readEditorSemanticContext`/`applyEditorPlan`/`runEditorPlanCommand` so any planner runs as one CodeMirror transaction. Added the `@fishmark/editor-model` alias to the renderer tsconfig and both Vite/Vitest configs, and registered the two bridge import exceptions in the guard manifest (`retireIn: RF-604`). 5 focused bridge tests. The field is deliberately not installed in the extension list yet, so the shipped app is unchanged. | Focused bridge 5 tests; `packages/editor-core` + `packages/editor-model` 98 tests. | typecheck; lint 0 errors / 8 pre-existing warnings; build exit 0; full Vitest 194 files / 2,506 passed + 1 skipped; architecture guard 234 tests. | Preparation only: RF-506 still needs the command routing, the ~8.2k-line deletion and the test replacement. | RF-506 remains PLANNED; M5 is 5/6. |

| 2026-08-14 | RF-506 (preparation) | Measured the cutover parity gap by replaying every `fixtures/editor-behavior` case's `checkpoints[0].actions` through the pure planners and diffing source+selection against the recorded result: 71 of 121 primary checkpoints match. Matched by command: `selection` 25/25, `Backspace` 12/14, `ArrowUp` 11/12, `ArrowDown` 10/12, `InsertText` 5/6, `Enter` 4/26, `Tab` 3/12 (+3 unclaimed), `Shift+Tab` 1/11. The gap is semantic, not structural: the missing rule is the legacy blank-line separator (a paragraph break writes `\n\n`; a break inside a blockquote writes `\n` + bare `>` separator + `\n` + quote prefix, so `> alpha` + Enter must give `> alpha\n>\n> `; a list item keeps its own prefix and ordinal), and navigation/Backspace must treat those separator lines as structural. | No code landed — the probe was throwaway. | Full suite unchanged at 194 files / 2,506 passed + 1 skipped. | Preparation only: close these gaps in the planners before switching routes, or `src/renderer/code-editor.test.ts` regresses on real behaviour. | RF-506 remains PLANNED; M5 is 5/6. |

| 2026-08-14 | RF-506 (preparation) | Ported the legacy semantic rules the corpus enforces into the pure planners: the paragraph blank-line break (`\n\n`, plus an extra blank before following content and the line-start variant), the blockquote paragraph break (`\n` + separator + `\n` + continuation, bare marker for one level), unterminated `>` marker completion, empty-quote exits with adjacent-pair collapse, `Tab` semantics (complete-and-indent an unterminated marker, indent a non-first item, close invisible empty quote lines first, leave a scope's first item alone), and the trailing blank-line collapse on Backspace. | Focused `packages/editor-model` 103 tests. | typecheck; lint 0 errors / 8 pre-existing warnings. | Replay parity against the enforced primary checkpoints moved from 93/112 to 105/112 matched; the 7 open cases are four list-scope Enter cases, the fence-opener draft Enter, and two arrow moves across structural separators. | RF-506 remains PLANNED; M5 is 5/6. |

| 2026-08-14 | RF-506 (preparation) | Finished the list- and fence-level replay parity: an empty list item now leaves through the container's separator (`>` + `\n` + `> ` inside a quote, `\n` at top level) instead of leaving a padded separator, and drops to its parent scope's level when its own list is nested inside an item; a list item whose caret sits at its content start hands its tail to a new paragraph (`1. Previous\n\nBody`); Tab closes the invisible empty quote lines above a first-in-scope item before indenting it, completes an unterminated marker, and leaves a scope's first item alone; Backspace collapses a trailing blank-line run; and an unclosed fence opener gains its content line and closing marker. | Focused `packages/editor-model` 104 tests; `packages/editor-core` 405 tests. | typecheck; lint 0 errors / 8 pre-existing warnings. | Replay parity against the enforced primary checkpoints moved from 105/112 to 109/112 matched; the three open cases are two arrow moves across structural separators and one deepest-ordered-list Enter case. | RF-506 remains PLANNED; M5 is 5/6. |

| 2026-08-14 | RF-506 (preparation) | Closed the last replay-parity gaps: vertical arrows now step over structural separator lines (blank lines, whitespace-only lines, and bare quote markers) instead of landing on them, and moving up across one lands at the end of the visible line above; Enter on an indented line the recursive tree reads as a paragraph continuation continues it as a list item at its own scope's indent, which is how the legacy indentation scopes see it. | Focused `packages/editor-model` 104 tests; `packages/editor-core` 405 tests. | typecheck; lint 0 errors / 8 pre-existing warnings. | Replay parity against the enforced primary checkpoints is now 112/112 matched (was 109/112). | RF-506 remains PLANNED; M5 is 5/6. |

| 2026-09-19 | RF-506 (slice fifty-four) | Delegated the arrow-shape classification to a subagent to save context and it **failed**: after two rounds it returned nothing (no `arrow-classify.json` was ever produced) and had stopped at "about to apply the temporary wiring", leaving **three un-reverted lines** in `code-editor.ts` (`onSemanticCommands: () => {}` and `onSemanticChangeFrame` inside `createFishMarkMarkdownExtensions`). Left in place, that wiring permanently enables the semantic surface and would silently corrupt every later measurement. Interrupted the agent and removed the residue by hand. | Verified the workspace is back to slice fifty-three's clean state: `git diff --stat -- src/renderer/code-editor.ts` empty, zero probe matches in `markdown.ts`, typecheck exit 0, **673/673** across 34 files, lint exit 0, build exit 0. No net change this round. | Reconfirmed that `markdown.ts` holds only RF-506's pre-existing optional wiring (43+/2−). | Slice fifty-four complete. | Next: do the arrow classification in-session — apply the temporary wiring, capture the switched failure list, `read` each case's source and assertions, build the shape table, pin one unit case per shape, and only then change `planVerticalNavigation`. |

| 2026-09-19 | RF-506 (slice fifty-three) | Switched to the arrow family and first re-checked slice twenty-two's "two cases want opposite landing strategies". Reading both tests shows they **do not conflict**: `enters the last visible blockquote line…` (source `> Quote line`/`> Still quoted`/blank/`Paragraph`, from 27) wants ArrowUp to land on the target line's **content start** (15), while `skips the collapsed structural blank row…` (source `Paragraph one`/blank/`Paragraph two`, from 13) wants ArrowDown to land on the **end** of the next paragraph (27). Different directions, different target lines — slice twenty-two read two different shapes as one. Also measured the family baseline: switching `ArrowUp`+`ArrowDown` leaves **16** failures. Pinned both shapes in `navigation.test.ts` — the ArrowUp case records the planner's current landing (end of `Still quoted` = 26) with the corpus expectation in a comment, the ArrowDown case records the corpus expectation (27) which the planner already meets. | Then tried ArrowUp landing on `targetLine.contentStartOffset`: the new unit case went green but the switched corpus went **16 → 17**, a net minus one, so it was reverted. No code diff outside the two new unit cases. 34 files / **673/673** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0; `code-editor.ts` and `navigation.ts` have no diff. | Slice fifty-three complete. | Next: classify all 16 arrow failures by shape first (which want content start, which want end) and pin one unit case per shape before touching `planVerticalNavigation`; changing it from a single case is what produced this round's −1. |

| 2026-09-19 | RF-506 (slice fifty-two) | Attempted item 4 of the Backspace leftovers (the fenced-code case) twice and **neither attempt moved any metric** — switched count stayed 5 both times — so both were reverted. Measured the case precisely: source ` ``` ` / `code block` / ` ``` ` / blank has **length 19**, physical lines are `1:r0,4 c0,3` / `2:r4,15 c4,14` / `3:r15,19 c15,18`, the planner's first press produces the right text with caret 18, and the test expects anchor 14 (`indexOf("code block") + 10`). Tried "caret = previous line's content end" and "caret pulled back only when the offset sits on the line's break"; both left the corpus at 5, i.e. my predicate does not match the real inputs (same class of mismatch as slice fifty's unit-vs-corpus divergence). | Concluded that the **whole Backspace family is now stop-loss**: the 15 → 5 gain is fully banked, and each remaining item has a named blocker that resists single-point patching (normalizer scope: three failed attempts; multi-press shape: needs state-machine modelling; fence caret: two failed attempts on a branch slice forty-two already tuned; the fifth item belongs to Enter). 34 files / **671/671** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0; `code-editor.ts` has no diff. | Slice fifty-two complete; Backspace family closed at 5. | Next: switch families — the arrow family (≈12, after resolving the two conflicting landing strategies from slice twenty-two) or the Enter table-draft cases (≈3, new capability). |

| 2026-09-19 | RF-506 (slice fifty-one) | Consolidated the remaining `Backspace` failures instead of gambling on another patch. Classified all five: (1) `breaks ordered list rendering…` and (2) its quoted twin — the normalizer mis-scope, **three attempts already failed** (−7, −2, 0), marked stop-loss; (3) `removes an empty nested quote list marker…` — a **multi-press** shape needing state-machine-level modelling (slice thirty-six); (4) `deletes code content directly…below a fenced code block` — **text already correct, cursor off by 4** (expected 14, actual 18), the lowest-risk item; (5) `creates a structural separator…when Enter exits from the last row at document end` — attributed to Backspace's switched run but named for Enter, ownership to be settled. | No code change; `Backspace` stays at **5** and `code-editor.ts` has no diff. Recorded that item 4's fix must treat *every* user of `planTrailingBlankCollapse` as one constraint set first, because slice forty-two won that branch's behaviour and slice forty-three already showed a caret change there can regress a family. 34 files / **671/671** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0. | Slice fifty-one complete. | Next: attempt item 4 under the "list all users of the branch first" rule; if those constraints conflict, mark it stop-loss like items 1–2 and move to the arrow family. |

| 2026-09-19 | RF-506 (slice fifty) | Third fix attempt on the ordered-rendering case, and it exposed a path divergence: clamping the normalizer's scope to the changed line (treating "change starts a line" as "that line's marker was removed") **did change the unit result** (`1. 内容\n内容2\n1. 内容3` → `1. 内容\n内容2\n2. 内容3`) but left the switched corpus at **5** with the target cases still red and no regressions. So the corpus does not reach that code with `changedRanges: [{from:6,to:9}]` — the real filter passes a different range, most likely from the `follows=1` pass slice forty-five observed running **before** the semantic transaction. | Reverted; `Backspace` stays at **5**, `code-editor.ts` and `list-edits.ts` have no diff, and `list-edits.test.ts` keeps only slice forty-nine's recorded case. Three attempts on this case now total −7, −2, 0. 34 files / **671/671** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0. | Slice fifty complete; this case is now marked stop-loss. | Next: either print `readTransactionChangedRanges(transaction)` plus the normalizer's actual input in `markdown.ts` so the unit probe matches the corpus **argument-for-argument**, or leave this family and take the arrow family (≈12) and the Enter table-draft cases (≈3), locking in the Backspace 15 → 5 gain. |

| 2026-09-19 | RF-506 (slice forty-nine) | Built the unit-level safety net slice forty-eight asked for, without changing behaviour. `list-edits.test.ts` gains `records how a marker-less line splits the ordered scope`, feeding `computeNormalizedOrderedListDocument` the post-delete intermediate (`1. 内容\n内容2\n3. 内容3`, `changedRanges: [{from:6,to:9}]`) and pinning today's wrong answer (`source: "1. 内容\n内容2\n1. 内容3"`, `changes: [{from:10,to:12,insert:"1."}]`); the comment records that the desired result is `null`. That way the next round flips one assertion instead of re-deriving the case from the corpus. | No behaviour change; `Backspace` stays at **5**, `code-editor.ts` has no diff. 34 files / **671/671** passed (one new unit case). | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0. | Slice forty-nine complete. | Next: inside `computeNormalizedOrderedListDocument` / `computeChangedRangeOrderedListNormalization`, detect a marker-less line inside the candidate range and stop renumbering across it; then flip the new assertion to `toBeNull()` and re-check the switched corpus for both the two target cases and the two `renumbers … when deleting a middle item` cases. |

| 2026-09-19 | RF-506 (slice forty-eight) | Tried slice forty-seven's preferred fix — a `deletesOrderedListMarker(transaction)` guard in `markdown.ts` that skips ordered normalization when the edit removes a line-leading ordered marker, judged from the pre-edit text. It **regressed**: switched-configuration `Backspace` went **5 → 7**, the two target cases stayed red, and two `renumbers … when deleting a middle item` cases broke (`5. first\n7. third` instead of `6. third`). Narrowing the guard to exclude whole-row removals (removed text starting with a newline) left it at 7, so that was not the distinguishing factor. | Reverted everything; `Backspace` stays at **5** and both `code-editor.ts` and `list-edits.ts` have no diff. Conclusion recorded: the discriminator cannot be the edit's *shape* (which characters were deleted) but must be whether the edited region **still forms an ordered list** — the target cases leave a marker-less line that breaks the list, while the regressed cases keep markers on every remaining item. 34 files / **670/670** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0. | Slice forty-eight complete; no gain. | Next: fix it at the unit level instead — add two `list-edits.test.ts` cases (a broken intermediate must return `null`; a normal intermediate must renumber), get the predicate right there, and only then wire it into `markdown.ts`. |

| 2026-09-19 | RF-506 (slice forty-seven) | Took slice forty-six's entry point and probed `computeNormalizedOrderedListDocument` **directly in a unit test**, bypassing the editor. Feeding it the semantic delete's intermediate (`1. 内容\n内容2\n3. 内容3`, `changedRanges: [{from:6,to:9}]`) returns `{"source":"1. 内容\n内容2\n1. 内容3","changes":[{"from":10,"to":12,"insert":"1."}]}` — exactly the wrong text seen in the editor, so the root cause is confirmed inside that function. Why it errs: the intermediate's second line has no marker, the parser reads the whole thing as **one list whose second item is a lazy continuation**, so `appendOrderedListScopeChanges` walks two items and rewrites the tail's `3.` to the scope's start ordinal `1.`, re-joining the list. | No code change; `Backspace` stays at **5** and `list-edits.test.ts` has no diff (72/72). Recorded the blocker and the two candidate fixes: pass the *deleted text* (or a "marker was deleted" flag) into the normalizer, or — smaller, and preferred — detect that case in `markdown.ts` and **skip ordered normalization** for it, using the existing `orderedListNormalizationAnnotation` escape hatch, after confirming it does not break the cases that rely on this normalizer. 21 files / **478/478** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0. | Slice forty-seven complete. | Next: implement the skip-on-marker-delete route in `markdown.ts` and verify against both ordered-rendering cases plus the existing normalization dependents. |

| 2026-09-19 | RF-506 (slice forty-six) | Tagged both follow-up sites and hit on the first run: `RF508SITE ordered [10-12:"1."]` — the rewriter is **`computeNormalizedOrderedListDocument`** (`markdown.ts:1087`), not the hidden-marker selection site. Also corrected the case's actual input: it is a **three-item** ordered list (`1. 内容` / `2. 内容2` / `3. 内容3`, caret at `indexOf("内容2")`) expecting the list to **break** there (`1. 内容` / blank / `2.内容2` / `3. 内容3`). The chain is now complete: the planner's delete of `[6,9)` yields the correct broken intermediate (`1. 内容\n内容2\n3. 内容3`), then the ordered-list normalizer renumbers the third item and re-joins the list. | No code change; `Backspace` stays at **5** and `code-editor.ts` has no diff. Recorded the next entry point: `list-edits.ts`'s `computeNormalizedOrderedListDocument` must treat a line whose marker was just deleted as a **list boundary** rather than a renumberable member; verify together with the quoted twin case. 21 files / **478/478** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0. | Slice forty-six complete. | Next: fix `computeNormalizedOrderedListDocument` (or its `changedRanges` handling) and check both the plain and quoted variants. |

| 2026-09-19 | RF-506 (slice forty-five) | Ran step two of the four-step method and pinned the rewriter. Three prints for one keypress, in order: `RF508FILT follows=0 effectiveLen=19 docChanged=false`, `RF508FILT follow [10-12:"1."]`, `RF508FILT follows=1 effectiveLen=16 docChanged=true`, then `RF508RUN applied changed=16`. So (a) there is **no double dispatch** — the semantic run reports once — and (b) the follow-up that rewrites the result is emitted by a filter pass that runs **before** the semantic transaction. Its change is `[10,12) → "1."`, i.e. it removes the space between an ordered marker and its content, which is the same edit family the corpus expects (`2.内容2`). | No code change; `Backspace` stays at **5**, and `code-editor.ts` / `semantic-keypress.ts` have no diff. Recorded the next step: tag each of the three `followUpTransactions.push` sites (ordered-list normalization / hidden-marker selection / structural navigation) to find which one emits `[10,12) → "1."`, and check why it runs without a document change — `shouldNormalizeHiddenMarkerSelection` holds whenever a selection is present, which is the leading suspect. 21 files / **478/478** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0. | Slice forty-five complete; the fix location is now `markdown.ts`, not `backspace.ts`. | Next: tag the three follow-up sites and re-run this case. |

| 2026-09-19 | RF-506 (slice forty-four) | Ran step one of slice thirty's four-step method on the ordered-rendering case and got decisive data: the transaction immediately before `view.dispatch` is **`[6,9)`**. For the Chinese source that range is the newline plus the second line's first two characters, producing `1. 内容2\n1. 内容3`, whereas the corpus sees `1. 内容\n内容2\n1. 内容3` — so the result **is** rewritten after the planner. This also explains slice forty-three's contradiction: the ASCII probe used one space after the marker while the real case has a different width, so the two probes were not equivalent inputs. | No code change; `Backspace` stays at **5** and both `code-editor.ts` and `semantic-keypress.ts` have no diff. Recorded step two and the three suspects, flagging the third as most likely and same-shaped as slice fifteen's Enter root cause: if `semanticCommands.run` is not reported as handled, CodeMirror runs its **default Backspace** on top of the semantic edit. 21 files / **478/478** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0. | Slice forty-four complete. | Next: print the transaction filter's returned spec (with follow-ups) at the filter exit, and first rule out a double dispatch by checking what the semantic `run` returns for this keypress. |

| 2026-09-19 | RF-506 (slice forty-three) | Took the two ordered-rendering cases and narrowed the problem without gaining. A planner probe on an ASCII equivalent (`1. AAAA\nBBBB\n1. CCCC`, anchor 3) shows `marker-degrade` deleting `[3,5)` — the marker plus its spacing — which matches the expected text, yet the real corpus leaves the marker in place (`1. 内容\n内容2\n1. 内容3`), so the divergence sits **after** the planner. A caret change on that branch (adding a `caret` parameter to `rangeDelete`) went **5 → 12**, breaking eight cases in the empty-marker family, so it was reverted. | No net change; `Backspace` stays at **5** and `code-editor.ts` has no diff. Recorded the next step: use slice thirty's four-step method on these two cases (print the transaction before dispatch, then the filter's output spec) instead of editing `backspace.ts`. 21 files / **478/478** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0. | Slice forty-three complete. | Next: instrument the semantic path for `breaks ordered list rendering…` — confirm the pre-dispatch range, then find which filter branch rewrites it. |

| 2026-09-19 | RF-506 (slice forty-two) | Largest Backspace gain in this family. A planner probe over `# Title\n\n\n\n` (`7 D 4`, `8 J 3`, `9 J 2`) showed `planTrailingBlankCollapse` deleting the **entire** trailing run in one press via `previous.contentEndOffset`, while the corpus semantics are one repeated empty paragraph per press (anchor 9 must leave three newlines, not two). A first cut that uniformly removed one line per press reached 7 but **broke two quoted cases** (`joins a trailing empty quote line across its structural separator on Backspace`, `deletes trailing empty quote lines after a quoted list on Backspace`), because a trailing run of **bare quote separators** must still collapse whole. Splitting on `isBareQuoteSeparator` gives both behaviours. | Switched-configuration `Backspace`: **8 → 5**, three cases green, no new failures (cumulative 15 → 5). Added `backspace.test.ts` case `removes one repeated empty paragraph per press below a heading`. 21 files / **478/478** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0; scaffolding reverted (`code-editor.ts` has no diff). | Slice forty-two complete. | Next on the five remaining Backspace cases: the two ordered list/quote rendering cases, `removes an empty nested quote list marker…` (slice thirty-six's three-press shape), `deletes code content…below a fenced code block`, and `creates a structural separator…when Enter exits from the last row at document end`. |

| 2026-09-19 | RF-506 (slice forty-one) | Switched families per slice forty's stop-loss and took a real gain in the Backspace blank-run family. A planner probe over `Alpha\n\n\nBeta` (`5:default | 6:subtree-join:"Alpha\n\nBeta" | 7:subtree-join:"Alpha\nBeta" | 8:subtree-join:"Alpha\nBeta"`) showed `joinSourceOffset`'s `separatorCount > 1` branch jumping to the line **before the whole run**, deleting the entire run in one press; one press must remove one line, so both paths now return `line.range.startOffset - 1`. | Switched-configuration `Backspace`: **9 → 8**; `removes a visible extra blank row before joining across the structural separator on Backspace` goes green, no new failures. Added `backspace.test.ts` case `shortens a run of separators by one line per press`. 21 files / **477/477** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0; scaffolding reverted (`code-editor.ts` has no diff). | Slice forty-one complete. | Two threads for next: (a) anchor 7 of the same input still deletes two breaks (`Alpha\nBeta`) where the corpus shape says one, so the `separatorCount` split needs explaining before more edits; (b) `removes one repeated empty paragraph with one Backspace from consecutive trailing blank lines` goes through `planTrailingBlankCollapse`, a different branch, and can be taken separately. |

| 2026-09-19 | RF-506 (slice forty) | Tried both remedies slice thirty-nine proposed for the extra blank line on empty-item exit, and **both failed**. Computing the exiting item's own run start (so the delete range omits the break before the line) took switched-configuration `Enter` from **13 to 24** — eleven new failures, all in `exits …` / `upgrades … at content start` / `keeps input.list-exit …`. The reason is now clear: `replaceLineDecision`'s `from` deliberately covers the whole line run **including the break before it**, and the branch's `prefixText`/`carried` are assembled for that meaning, so moving the start changes every case that shares the branch. A narrower variant failed the same way. | Reverted both; `Enter` stays at **13** and `code-editor.ts` has no diff. Recommendation recorded: leave these two cases for now and move to better-understood families, because fixing them means redefining the exit branch's line-replacement semantics (a refactor) rather than a local patch; if returning later, first add unit tests pinning the thirteen currently-green exit cases. 21 files / **476/476** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0. | Slice forty complete; no gain, and this sub-family is now marked stop-loss. | Next: switch families — Backspace blank-run/ordered-list (≈3) or the arrow family (≈12, after resolving slice twenty-two's conflicting landing strategies). |

| 2026-09-19 | RF-506 (slice thirty-nine) | Found that two remaining Enter cases share one cause — the outermost empty item's exit leaves **one newline too many** (`1. Todo\n\n\n3. Todo2…` vs `1. Todo\n\n3. Todo2…`; `- [ ] todo\n\n\n` vs `- [ ] todo\n\n`) — because `replaceLineDecision` deletes from `line.range.startOffset`, which includes the break before the line, and the branch then inserts another `"\n"`. Tried deleting from `prefix.startOffset` instead; that went **13 → 15** and broke two quoted cases (`exits a quoted list without leaving trailing quote separator rows`, `exits an empty top-level quote list item to quote body on Enter`) because inside a quote `prefix.startOffset` sits after the `>` and the deletion then eats the marker. | Reverted; switched-configuration `Enter` stays at **13** and `enter.test.ts` is 23/23. Narrowed the next attempt to two options, preferring the smaller one: keep the delete range and **stop inserting `"\n"`** in that branch (the range already consumes the break above), or use `prefix.startOffset` only when `prefix.ancestorText.length === 0`. 21 files / **476/476** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0; scaffolding reverted (`code-editor.ts` has no diff). | Slice thirty-nine complete; no gain. | Next: try the "do not insert the extra `\n`" variant, verifying the two quoted exit cases as well as the two blank-block cases. |

| 2026-09-19 | RF-506 (slice thirty-eight) | Took slice thirty-seven's recommended family (Enter empty-item upgrade) and found a data-loss bug in slice twenty's promotion branch: `planListItemEnter` rebuilt the line from ancestor text, indentation, marker and marker spacing but **omitted `prefix.taskText`**, so an empty nested task item lost its checkbox on promotion (`- [ ] parent\n- ` instead of `- [ ] parent\n- [ ] `). Adding `prefix.taskText` fixes it; for non-task items the string is empty so behaviour is unchanged. Also located why the quoted sibling still fails: a probe shows the planner returns **`quote-continue`**, not a list decision, because the tree holds **no list-item node** for an indented list item inside a blockquote — a structural gap shared with slice twenty's flat-tree finding, not a rule gap. | Switched-configuration `Enter`: **14 → 13**, one case green, no new failures. Added `enter.test.ts` case `keeps the checkbox when an empty nested task item is promoted`. 21 files / **476/476** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0; scaffolding reverted (`code-editor.ts` has no diff). | Slice thirty-eight complete. | Next: continue the empty-item family on the cases that do have list-item nodes (`upgrades an empty top-level ordered list item before later siblings to a body blank block`, `keeps the caret on the visible blank line when exiting an empty top-level item…`, `keeps a structural separator when exiting an empty task item at EOF`); treat "list recognition inside a blockquote" as its own structural item. |

| 2026-09-19 | RF-506 (slice thirty-seven) | Re-measured the merged switched-configuration baseline because the per-command picture had shifted since slice thirty-five: **all six commands = 39 failures**, `Enter` alone = **14**, `Backspace` alone (post slice thirty-five) = **9**. Classified the 39 by feature family to set priority: arrows × blank-rows/tables ≈12, Enter × empty-item upgrade and table draft ≈11, Backspace × blank-run collapse and ordered lists ≈9, fence/composite fixtures ≈4. Recommended taking the Enter empty-item family first because it shares the rule slice twenty already proved, and treating arrows last because slice twenty-two recorded two of its cases demanding **opposite** landing strategies. | No code change; everything reverted and `Backspace` stays at **9**. Recorded one process violation: I edited `markdown.ts`'s switch array with a PowerShell regex, the exact pattern that corrupted that file three times before. It happened to survive (typecheck clean) and was restored with the edit tool, but the rule stands. 21 files / **475/475** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0; `code-editor.ts` has no diff. | Slice thirty-seven complete; baseline refreshed. | Next: take the Enter empty-item-upgrade family (≈5 cases) using the slice-twenty rule, probing real anchors first as slices thirty-one through thirty-five established. |

| 2026-09-19 | RF-506 (slice thirty-six) | Reconnaissance only, no code change. The next Backspace case, `removes an empty nested quote list marker before clearing its indentation on Backspace`, is a **multi-press** shape: one `it` presses Backspace three times and asserts after each (`>   - ` → `>   ` → `>  ` → `> `), i.e. it grades recursive convergence on one shape — first the list marker degrades, then the indent spaces are eaten one per press. Every case fixed so far has been single-press, so a rule cannot be written from the first press's expectation alone; fixing one press changes the input to the next. | Measured current behaviour: the switched configuration produces `> - parent\n>   -` (it drops the trailing space instead of the marker), which already mismatches press one's expectation. Everything reverted; `Backspace` stays at **9** and `code-editor.ts` has no diff. 21 files / **475/475** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0. | Slice thirty-six complete; no gain. | Next: probe all three presses separately (anchor and legacy edit before/after each `pressBackspace`, since the document changes between them), make press one correct, then re-verify presses two and three rather than assuming they follow. If the three presses demand mutually exclusive rules, mark the case as needing a multi-press state machine and move to another family. |

| 2026-09-19 | RF-506 (slice thirty-five) | Implemented the missing branch slice thirty-four identified: for the leading run of a **later** line of a blockquote (decided by `isFirstLineOfQuote`), Backspace now returns a plan with **no edits** and moves the caret to the end of the line above, matching the legacy path's selection-only dispatch. Two probes confirmed the work rather than guessed it: the branch probe read `atLead=true ln=4 off=25 cstart=25 first=false` (so the branch fires, unlike the guard-only attempt), and the first cut — which used `offset` as the caret — failed with `expected 25 to be 22`, proving the text was already right and only the caret was wrong. | Switched-configuration `Backspace`: **12 → 9**, three cases green, no new failures. Added `backspace.test.ts` case `only moves the caret back on a later line of the same quote`, asserting kind, unchanged document, and the caret on the previous line's content end. 21 files / **475/475** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0; scaffolding and probes reverted (`code-editor.ts` has no diff). | Slice thirty-five complete. | Next: apply the same two-probe recipe (real anchor, then legacy edit) to `removes an empty nested quote list marker before clearing its indentation on Backspace` (anchor 17, produces `> - parent\n>   -`, expects `> - parent\n>   `), then move to the ordered-list, table, fence, and blank-run families. |

| 2026-09-19 | RF-506 (slice thirty-four) | Implemented slice thirty-three's "first line of its blockquote" discriminator via `lineContainerChain` and the container's first covered physical line. It works as written but **does not fix the case**: switched-configuration `Backspace` stays at **12**. The reason is now precise — a guard on the branch that deletes the preceding break only prevents that branch from firing, after which execution falls through to `quote-degrade`, which is exactly the branch producing the wrong result. What that shape needs is a plan with **no edits at all** (caret move only), matching the legacy path's complete absence of a document change. | Reverted the attempt; `code-editor.ts` has no diff and `Backspace` stays at **12**. Recorded the corrected implementation plan (emit an empty-edit plan for "later quote line + caret at content start" **before** step 3, keep `isFirstLineOfQuote` for the first-line branch). 21 files / **474/474** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0. | Slice thirty-four complete; no gain. | Next: add the empty-edit branch rather than another guard. Note for planning: this quote family has now had three consecutive non-gaining rounds (−4, −1, 0); if the empty-edit branch also fails, stop and build unit tests for the quote-structure predicate first instead of continuing to spend rounds here. |

| 2026-09-19 | RF-506 (slice thirty-three) | Implemented slice thirty-two's corrected blank predicate (newline-free boundary) — it now works, but the whole direction was wrong: switched-configuration `Backspace` went **12 → 13** because the guard also suppressed the case slice thirty-one had fixed, and that case *also* has a blank line above it. So "is the previous line blank" **cannot** be the discriminator. A legacy-side probe settled it: for `allows leaving a blockquote…` the legacy deletes `10-11:""` (the break before the line), while for `keeps blockquote presentation…at a later line start` the legacy performs **no document change at all** — its test only asserts unchanged content plus a caret move, yet the semantic path deletes the marker and space. | Reverted everything; `Backspace` stays at **12** and `code-editor.ts` has no diff. Recorded the real discriminator: whether the caret's line is the **first line of its blockquote** (A: first line ⇒ delete the break; B: later line ⇒ leave the document alone). 21 files / **474/474** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0. | Slice thirty-three complete; no gain, but the true rule is now identified. | Next: implement "is this the first line of its blockquote" (compare the line start with the quote container's `source.startOffset` from `lineContainerChain`) instead of any predecessor-blank test, and verify against the two recorded cases plus the switched corpus. |

| 2026-09-19 | RF-506 (slice thirty-two) | Measured the real anchors of the three remaining same-family cases with the `setSelection` probe: `16 → 16`, `23 → **25**`, `17 → 17` — the middle one confirming again that a caret on a hidden marker is normalized to the content start. A planner-side probe (`off=25 lstart=23 cstart=25 ln=4 lastMarker=23 ownC=25`) showed slice thirty-one's `atLeadingRun` branch does fire there, so slice thirty-one's fix is **too broad**: it deletes the preceding break both when the line above is a blank line and when it is ordinary text, while the two shapes need different results. A refinement guarded by `previousIsBlank` went **12 → 16** (net minus four, regressing three previously-green cases) because `lineTextOf` includes the newline, so a blank line's slice is not empty. | Reverted the refinement; switched-configuration `Backspace` is back to the slice-thirty-one value of **12**. Recorded all three anchors and their actual-versus-expected text as ready-made unit cases. 21 files / **474/474** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0; probes reverted, `code-editor.ts` has no diff. | Slice thirty-two complete; no gain. | Next: decide "is the previous line blank" using a newline-free boundary (`context.source.slice(prevPrefix.contentStartOffset, previous.contentEndOffset)`), keep the break-deleting branch only for a non-blank predecessor, and let a blank predecessor fall back to the existing marker-degrade. Verify against the three recorded anchors, then the switched corpus. |

| 2026-09-19 | RF-506 (slice thirty-one) | Explained the 11 → 13 caret shift and turned it into a fix. A probe right after `setSelection`'s dispatch reports `RF508SEL requested=11 actual=13`: `normalizeHiddenSelectionAnchor` (`markdown.ts:1132`) pushes a caret off a hidden quote marker onto that line's content start within the same dispatch, and it is not semantic-path specific — the legacy path sees the same 13. So the rules must read the normalized caret. Backspace step 3 now treats **line start or content start** as the leading run and deletes the break before the line (`lineStart - 1`), matching the legacy edit. | Switched-configuration `Backspace`: **13 → 12** (cumulative 15 → 12), one case green, no new failures. Added `backspace.test.ts` case `steps a quoted line up when the caret is at its content start`, anchored at the content start because that is what the real path hands the planner. 21 files / **474/474** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0; scaffolding and `setSelection` probe reverted (`code-editor.ts` has no diff). | Slice thirty-one complete. | Next: apply the same recipe — read the **real normalized anchor** with a `setSelection` probe first, then write the rule — to the three remaining same-family cases (`keeps blockquote presentation…at a later line start`, `keeps compact nested blockquote content intact…at content start`, `removes an empty nested quote list marker…`). |

| 2026-09-19 | RF-506 (slice thirty) | Ran the four-step verification and **located** slice twenty-nine's contradiction. Printing the transaction immediately before `view.dispatch` gave `RF508PRE applied 11-13:""`, so the planner really produces `[11,13]` — the transaction filter does not rewrite it, disproving slice twenty-nine's hypothesis. Printing the planner's context gave `RF508CTX off=13 lineStart=11 contentStart=13 ln=3 lastMarker=11`: the planner is handed **anchor 13**, not the 11 the test sets on the `>` marker, so `offset === contentStart` holds and step 3 `quote-degrade` deletes `[11,13)` (marker plus its spacing) instead of the break before the line. The slice-twenty-nine unit probe had called the planner *directly* at offset 11, which is why it disagreed with the real path. | Nothing kept; every change reverted. Switched-configuration `Backspace` stays at **13**. 21 files / **473/473** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0; `semantic-keypress.ts` and `code-editor.ts` have no diff. | Slice thirty complete; no gain, but the blocker is now named. | Next: find who moves the caret from 11 to 13 — print the anchor at `setSelection`, print `transaction.startState.selection.main.anchor` at the filter entrance, and run the same prints on the legacy path (which deletes `[10,11]` from the same keypress). Decide from that evidence whether the semantic rules should follow the adjusted caret or prevent the adjustment. |

| 2026-09-19 | RF-506 (slice twenty-nine) | Attempted the next Backspace case and hit a contradiction that stopped the work. For the leave-blockquote shape, the **legacy path** deletes `[10,11]` and the **semantic planner called directly** also produces `[10,11]` with identical text, yet the **semantic transaction on the real path** ends up deleting `[11,13]`. So the divergence happens *after* the planner. The suspect is the transaction filter: `shouldNormalizeHiddenMarkerSelection` (`markdown.ts:1320`) holds for any doc-changed transaction without its annotation, and the branch from line 1103 recomputes the anchor and emits a follow-up. Since both paths pass through the same filter, the divergence more likely comes from **different filter inputs** (e.g. the `userEvent` annotation) than from the filter rewriting arbitrarily. | Reverted the attempt; `code-editor.ts` has no diff and `backspace.ts` keeps only slices twenty-seven/twenty-eight. Switched-configuration `Backspace` stays at **13**. 21 files / **473/473** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0. | Slice twenty-nine complete; no gain. | Next: stop editing the planner. Print the prepared transaction's changes *before* dispatch, then print what the transaction filter returns (including follow-ups) for the same keypress, and compare the two paths' filter inputs — in particular the `userEvent` annotation — before changing any rule. |

| 2026-09-19 | RF-506 (slice twenty-eight) | Completed the bare-quote-separator family. Printing the legacy path's actual edit gave `RF508RANGE 5-7:""` for `> 11` / `>` / `> > 1`, i.e. it deletes only the separator row, while the equal-depth case (`> 11` / `>` / `> 222`) deletes from the line above's content end. `planBareSeparatorJoin` now branches on quote depth: equal depths take the reach-back range, unequal depths delete just `separator.range` so the deeper line keeps its level. | Switched-configuration `Backspace`: **14 → 13**; both sibling cases green, no new failures. Added `backspace.test.ts` case `keeps a deeper quoted line in place by deleting only the separator row`. 21 files / **473/473** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0; scaffolding and the print probe reverted. | Slice twenty-eight complete. | Next: keep using the proven method — print the legacy edit on the real keyboard path, read the stack for the owning module, port the rule — on the six remaining quote/blank Backspace cases. |

| 2026-09-19 | RF-506 (slice twenty-seven) | Ran the two outstanding exit-condition gates for the first time and located the legacy Backspace rule empirically. `npm run test:editor-behavior` → `cases=121/121 targets=2541 unexpected=0` (exit 0); `npm run test:editor-foundation` → 310/310 (exit 0). Both are green on the legacy path, so the cutover must *preserve* green rather than create it. The rule behind the bare-quote-separator family was found by printing inside `observeDocumentUpdate` and reading the stack: `mergeSameDepthBlockquoteAcrossStructuralSeparator` (`packages/editor-core/src/commands/blockquote-commands.ts:323`) deletes `separator.previousBlockEnd → contentStart` and guards on `previousParsed.quoteDepth === currentParsed.quoteDepth`. | Ported that range as `planBareSeparatorJoin` in `backspace.ts`. Switched-configuration `Backspace`: **15 → 14**, one case green, no new failures. Added `backspace.test.ts` case `joins quoted lines by dropping the bare quote separator between them`. 21 files / **472/472** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0; scaffolding and the `observeDocumentUpdate` probe fully reverted. | Slice twenty-seven complete. | Next: port the missing quote-depth guard so the sibling case (`> 11` / `>` / `> > 1`, depths 1 vs 2) is excluded from the new branch, exactly as the legacy path excludes it. The condition is already identified, so this is low risk. |

| 2026-09-19 | RF-506 (slice twenty-six) | Finally measured which branch the target case hits. With the input that matches the corpus exactly (`> 11\n>\n> 222`, caret `indexOf("222") === 10`), `decideBackspace` returns **`quote-degrade :: "> 11\n>\n222" :: 7`** — step 3 fires and deletes `[7,9]` (this line's own marker plus its spacing), leaving a marker on every line where the corpus expects the separator row to be swallowed. This also **disproves** the earlier claim that the case falls into step 1's default delete: line 3 has `contentStartOffset = 9` with segments `quote-marker@7-8, spacing@8-9`, and the caret at 10 is the content's first character, so `offset <= contentStart` is false and steps 2/3 could never have matched. Both earlier probes were simply wrong inputs. | Two attempts to wire a separator branch failed on the same mistake — treating `line.contentStartOffset` as "the content start" while the quote spacing is hidden prefix, so the caret at 10 is neither in the marker run nor at the content start. Recorded the two sibling cases' conflicting requirements (`[5,10]` vs `[5,8]`) as the next anchor. 21 files / **471/471** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0; all temporary edits reverted (`backspace.ts` holds only slice seventeen's join rule). | Slice twenty-six complete; Backspace not improved. | Next: stop guessing conditions — print the **legacy path's** final change ranges for both sibling cases on the real keyboard path, then derive the discriminator from those numbers (likely "how many quote levels sit below the separator": one level swallows the row, two levels delete only the row). |

| 2026-09-19 | RF-506 (slice twenty-five) | **Corrected slice twenty-four on two counts.** (1) There is no cross-test pollution: slice twenty-four compared runs from *different code states* (fix applied vs reverted) and read the difference as order dependence. With the code state held fixed, the case fails identically when run alone, with its three siblings, and in the whole file. (2) The bare-quote-separator branch never fired: a unit probe at the real anchor (`> 11\n>\n> 222`, offset 10) returns `default`, because the guard `offset > line.contentStartOffset` sees `10 > 9`; relaxing it to the quote-prefix column still returned `default`, so the function is not reached or an earlier branch wins — not established. The slice-twenty-four claim that both cases passed in isolation was a mis-recording. | Verified that `destroy()` (`markdown.ts:1023`) never calls `semanticCommands.releaseSession()` while the semantic surface only calls `bindSession` at creation — but this round *disproved* that it causes cross-test pollution, so its actual impact stays unverified and unrecorded as a conclusion. 21 files / **471/471** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0; `backspace.ts` back to the slice-seventeen state, all scaffolding cleared. | Slice twenty-five complete; three hypotheses disproved, Backspace not improved. | Next round must stop writing branches and hoping: add temporary branch-name tracing at every `return` in `decideBackspace`, print which branch the target case actually hits, and only then change code. |

| 2026-09-19 | RF-506 (slice twenty-four) | Corrected slice twenty-three's own probe (wrong source string: the real case is `> 11\n>\n> 222` at anchor 10, not `> 11\n>\n222` at 9) and landed the boundary-correct bare-quote-separator branch, which **passes both target cases when run in isolation**. The family count did not move (15 → 15) because of a newly found effect: the same case **passes alone and fails when run with its siblings**, and only under the switched configuration — the baseline configuration is 4/4. So the semantic `Backspace` wiring leaves **cross-test state** behind. | Recorded the correct line boundaries (`1:0,5:2,4 | 2:5,7:6,6 | 3:7,12:9,12`) and the rule that the delete range must run from `above.contentEndOffset` to the caret, not from `range` boundaries. The fix was reverted because it has no net gain and cannot be shown stable. 21 files / **471/471** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0; scaffolding reverted. | Slice twenty-four complete; Backspace not improved, but the measurement itself is now suspect. | Next, before any further corpus numbers: decide whether the pollution is a measurement artifact or a real defect — check whether `releaseSession()` is ever called on view teardown (`markdown.ts` only calls `bindSession` at creation) and whether `editorStructureCacheField` is rebuilt per view. If it is real residue it is itself part of RF-603 and must be fixed first, because every switched-configuration number rests on this measurement. |

| 2026-09-19 | RF-506 (slice twenty-three) | Attacked `Backspace` and **reverted all three attempts**. (1) Narrowing `lastQuoteMarkerStart`'s `markerRunLength` to spare the space after a quote marker was net minus one (15 → 16) and broke two existing `backspace.test.ts` cases, proving the space *is* consumed by design. (2) The bare-quote-separator branch, moved ahead of step 1, **does** fire — disproving slice eighteen's "never reached" reading — but the real blocker was the condition shape: `lineAt` maps the caret to a line whose `range.startOffset` includes the previous newline, so `offset !== line.range.startOffset` never held. Loosened, it fired but deleted `[4,8]` instead of `[4,9]`, a one-newline mismatch between blockquote-prefix and physical-line boundaries. | Recorded the reproducible 15-case `Backspace` failure list and two durable facts: the parity harness's `Backspace 7/7` **shares no case** with those 15 failures, so it has no diagnostic value for this command; and the quote-degrade space consumption is intended semantics. 21 files / **471/471** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0; scaffolding reverted (`code-editor.ts` and `markdown.ts` scaffolding both gone); `backspace.ts` diff holds only slice seventeen's join rule. | Slice twenty-three complete; Backspace not improved. | Next: stop guessing at the corpus level and first dump `context.lines.lines` boundaries (`lineNumber / range.startOffset / range.endOffset / contentStartOffset / contentEndOffset`) for `> 11\n>\n222` to learn which line object owns a blank line; that same mapping gap is the likely cause of the four remaining blank-row/empty-paragraph cases. |

| 2026-09-19 | RF-506 (slice twenty-two) | Attacked vertical navigation and **reverted it as a measured regression**. The rule read out of `semantic-parity.test.ts` ("after crossing a separator land on the target line's last visible character") raised the switched-configuration `ArrowUp`+`ArrowDown` failures from **15 to 17**, a net minus two, so `navigation.ts` is back at HEAD with no diff. The reason is now named: two real corpus cases demand opposite landing strategies for the same "crossed a hidden separator" shape — case 2324 expects the **visible column preserved** (`anchor 15`, the first character of `Still quoted`) while case 1465 expects the **end of the target line** (`anchor === source.length`). | Also corrected a measurement belief: the parity fixture's navigation expectations are **wrong**, not merely coarse — it records legacy `anchor 7` for `ArrowDown` over `alpha\n\n\nbeta` while the real corpus asserts `source.length`. Navigation must be judged by the real corpus only. 21 files / **471/471** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0; all scaffolding reverted. | Slice twenty-two complete; navigation not improved. | Next: leave navigation until the structural distinction between those two shapes is identified, and take the better-understood families first — `Backspace` (15, quote-separator interceptor already located at `decideBackspace` step 3) and `Enter`'s `empty top-level ordered before sibling` (a single surplus leading newline). |

| 2026-09-19 | RF-506 (slice twenty-one) | Fixed the `Tab` ordered-marker rule: an item indented into a scope of its own must start at ordinal `1`, but the planner only inserted indentation and kept the number the item held as a sibling. `decideIndentIn` now rewrites the marker and folds that rewrite into the same line's indentation edit — two separate edits at nearby offsets desynchronise under the adapter's back-to-front application (measured output `1.6. child`). | `indent.test.ts` gains two cases. Switched-configuration `Tab`+`Shift-Tab`: **3 → 1**. `semantic-parity.test.ts` matrix `Tab 2/3` → **`Tab 3/3`**, mismatches 11 → 10. 21 files / **471/471** passed. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0; scaffolding reverted (`code-editor.ts` has no diff). | Slice twenty-one complete. | Remaining `Tab`/`Shift-Tab` case is the outdent that must also move a continuation line at the item's own indent; that line is a sibling by indentation (4 == 4), so the legacy rule is not column-derivable. Establish the block-ownership concept the legacy path uses there. |

| 2026-09-19 | RF-506 (slice twenty) | Landed two root-cause fixes and rebuilt the measurement method. (1) **Flat-tree level rule**: the recursive tree stores indented list items as flat siblings under one `list`, so `parentListItemOf` is always null for them; `planListItemEnter` now drops one indentation run to promote an empty nested item and restores the `markerSpacingText` the first attempt dropped. (2) **Declared history events**: `EditTransactionPlan` gained an optional `userEventName`, `replaceLineDecision` sets `input.list-exit`, and `historyAnnotationFor` honours it ahead of `input.${commandId}` — `markdown.ts:1154` and undo grouping read that legacy name, and one corpus case failed on the annotation alone while its text was already correct. Measurement was replaced by a single scaffolding pass (one array controls which commands route to the semantic planner) because editing the keymap five times per run was the reason the numbers kept becoming irreproducible. | `enter.test.ts` gains three cases (2 pass, 1 records the still-open quote-nested shape). `transaction-adapter.test.ts` gains a `userEventName` contract case. Five suites: **353/353**. Switched-configuration failures: baseline 0, `Enter` 16→**14**, `Backspace` **15**, `Tab`+`Shift-Tab` **3**, `ArrowUp`+`ArrowDown` **15**, all six **45** (the single-command sum 47 exceeds 45, so cases overlap across commands). | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0; all measurement scaffolding reverted (`code-editor.ts` has no diff). | Slice twenty complete. | Next: the quote-nested shape is confirmed to take `quote-continue`, not `list-exit` — the tree holds **no list-item node** for `>   - ` at end of line, so `planEnter` step 3 never fires for it. Establish why that line has no list node, then attack `Tab`/`Shift-Tab`'s 3 remaining cases (the cheapest family) and the ordered-scope renumbering. |

| 2026-09-19 | RF-506 (slice nineteen) | Two Backspace attempts, both ineffective and both fully reverted. (1) Quote-separator family: confirmed the two cases do fail in the Backspace-only run, but a `decideBackspace` probe at the `222` line start of `> 11\n>\n222` returns `{ kind: "default", edits: [{ from: 8, to: 9 }] }`, which does not match the real behaviour. (2) Blank-run caret anchor: the target case expects `Alpha\n\n\nBeta` → `Alpha\n\nBeta` (one break removed) but the real path yields `Alpha\nBeta` (two breaks removed), so another branch (likely `planTrailingBlankCollapse`/`subtree-join`) handles that position first. | Full-corpus failures unchanged at 49; `semantic-parity` + `code-editor` + `markdown` = 315/315 after the reverts. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0. | Slice nineteen complete; no net change. | General lesson recorded in the decision log: a pure-planner unit probe cannot be extrapolated to the real keypress path because the transaction filter and renderer rewrite the semantic transaction's result; the only valid behavioural judge is the real `code-editor.test.ts` assertion output, with probes used solely to read field values. Next round must instrument the real path (print the final `prepared.frame.changes` and `decision.kind`) rather than infer from probes. |

| 2026-09-19 | RF-506 (slice eighteen) | Attempted the Backspace quote-separator family (two cases sharing one rule: `> 11\n>\n222` → `> 11222` and `> 11\n>\n> 1` → `> 11\n> > 1`). Added a branch that removes a bare quote-marker separator line together with the break that ends it, but the measurement showed both cases still failing and the Backspace failure count unchanged at 15, proving the new branch is never reached. Likely cause identified: step 3 of `decideBackspace` (quote-level degrade) also matches `offset <= contentStart` on a bare `>` line and intercepts first. The attempt was fully reverted so no unverified code remains. | `semantic-parity` + `code-editor` + `markdown` = 315/315 after the revert; Backspace stays at 15 failures. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0. | Slice eighteen complete; the quote-separator family is not fixed. | Next step is precise: probe `decideBackspace` directly on both inputs to print `decision.kind` and `edits` and confirm step 3 interception, then distinguish "quote-level degrade" from "remove the separator line" before step 3 while keeping `markdown.test.ts`'s quote assertions green. |

| 2026-09-19 | RF-506 (slice seventeen) | Attacked the Backspace line-start join family. Detail review showed the rule is "remove one line break per press", not "skip to the previous content": `Alpha\n\nBeta` at line start joins directly, while `Alpha\n\n\nBeta` only collapses one line. Implemented in `backspace.ts` by counting the contiguous separator lines above the caret (blank, whitespace-only, or quote-marker-only) and choosing the delete span accordingly. | Switching **only `Backspace`** on `src/renderer/code-editor.test.ts`: 16 failures before, **15 after**; `joins paragraph text across a structural blank separator on Backspace` and `joins body text after a list boundary into the previous item on Backspace` now pass with no new failures. Parity's Backspace row improved from 6/7 to **7/7**. | `semantic-parity` + `code-editor` + `markdown` = 315/315; lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0. | Slice seventeen complete; cutover not landed. | One case in the same family still differs only in caret anchor (`removes a visible extra blank row before joining across the structural separator on Backspace` expects 6, gets 7); recorded as the next precise target rather than spending further budget on it this round. No user-visible behaviour changed and no old semantic module was deleted. |

| 2026-09-19 | RF-506 (slice sixteen) | Re-measured the full-corpus口径 after slice fifteen's cross-command root-cause fix, because that fix affected the default-binding fallthrough for every semantic command, not just Enter. Switching all six commands now yields **50 failures, down from 57**, while switching only Enter yields **16, down from 20** — a total gain of 7 cases with no new failures. Also refreshed the keyword distribution of the remaining 50: Backspace 16, Enter 14, Tab 12, ordered 14, quote 13, ArrowUp 6, ArrowDown 4, table 9, blank line 6, fence 3 (cases can match several keywords, so this locates weight rather than summing). | `semantic-parity` + `code-editor` + `markdown` = 315/315 with the keymap restored. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0. | Slice sixteen complete; cutover not landed. | Priority order refreshed from data: Backspace quote/ordered family (16) → Enter remainder (14) → Tab (12) → vertical navigation and fence/table stragglers. No user-visible behaviour changed and no old semantic module was deleted. |

| 2026-09-19 | RF-506 (slice fifteen) | Found and fixed the root cause of the Enter failures. Instrumenting `code-editor.ts`'s `observeDocumentUpdate` produced a decisive comparison: the legacy path performs **one** transaction and yields the correct text, while the semantic path via `semanticCommands.run` yielded the wrong text, yet the same plan dispatched manually through `adapter.prepareCommand` + `view.dispatch` was **correct with one transaction**. That isolated the bug to `applyPreparedSemanticCommand` in `semantic-keypress.ts`: it returned `"stale"` whenever `recordEditorDispatch` did not answer `noted`, which made the keymap's `run` report the key as **unhandled**, so CodeMirror ran its default Enter binding on top of the edit already made — the source of the stray `\n`. Fixed so that a dispatched transaction always reports the key as consumed, reserving `unhandled` for plans that dispatched nothing. Also fixed a second bug: `createSemanticCommandBindings` only bound the adapter session when the host called `bindSession`, which the real `code-editor.ts` never does, so every `prepareCommand` returned `unhandled` and the same default-binding fallthrough occurred; the session is now bound at creation. | Switching **only `Enter`**: 20 failures before, **16 after** — four cases fixed in one change, with no new failures. `semantic-parity` + `code-editor` + `markdown` = 315/315 with the keymap restored. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0; all temporary instrumentation removed (`RF506` count 0). | Slice fifteen complete; cutover not landed. | This was a real root-cause fix rather than a per-case patch: cases previously polluted by the stacked default Enter (`splits a list item…`, `upgrades a nested list item to a parent item…`) now pass together. No user-visible behaviour changed and no old semantic module was deleted. |

| 2026-09-19 | RF-506 (slice fourteen) | Instrumented the real keypress path instead of running end-to-end switch experiments (temporary logs plus a temporary direct Enter route, all reverted). One run of the split test produced three decisive lines: the transaction filter first fires with `effectiveSource = "1. AlphaBeta\n2. Gamma"` (the original text, no follow-ups); the semantic planner prepares `edits: [{ from: 8, to: 8, insert: "\n2. " }]` with `resultingText = "1. Alpha\n2. Beta\n2. Gamma"`; the filter then fires again with `effectiveSource = "1. Alpha\n2. Beta\n3. Gamma"` and one follow-up. **The filter's final computed text is exactly what the corpus expects**, yet the assertion observes `1. Alpha\n2. \nBeta\n3. Gamma`, so the document is rewritten *after* the filter — either by a further transaction in the same keypress or by the renderer's frame/selection replay. | Enter failure count held at 20; `semantic-parity` + `code-editor` + `markdown` = 315/315 after full revert. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0. | Slice fourteen complete; the split case is not fixed, but the responsible region is now "after the transaction filter". | Next step: log every `docChanged` transaction (before/after text plus `userEvent` and `addToHistory`) in `code-editor.ts`'s `observeDocumentUpdate` and run the single test to see whether the keypress produces more than one document change, which would mean the semantic route is not marking itself as complete. |

| 2026-09-19 | RF-506 (slice thirteen) | Corrected slice twelve's conclusion. A unit-level probe (now deleted) passed five plausible `changedRanges` (`8→15`, `8→8`, `11→15`, `12→15`, `8→12`) for the planner output `1. Alpha\n2. Beta\n2. Gamma` into `computeNormalizedOrderedListDocument`; **every one returned the expected `1. Alpha\n2. Beta\n3. Gamma`**, so the normalizer cannot be the component that inserts the extra line break. The correct explanation for slice twelve's observation is that forcing `shouldNormalizeOrderedLists` to `false` makes the transaction filter **return early** at the guard, skipping everything after it — the hidden-marker and structural-navigation normalization branches (`markdown.ts:1099` onward) and the follow-up transaction construction. Slice twelve therefore only proved "the cause is after line ~1057". | Enter failure count held at 20; `semantic-parity` + `code-editor` + `markdown` = 315/315. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0. | Slice thirteen complete; the split case is not fixed. | Sharper hypothesis for the next round: with normalization **on**, the hidden/structural branches still run (no early return) and they *did* produce the correct result in the disabled run, so the likely culprit is the **combination** — the normalizer emits `sequential` follow-up transactions that are stacked onto the semantic Enter transaction, and the document shape between the two applications (or the renderer's frame merging in `code-editor.ts`) produces the extra break. Next step is to log `effectiveSource`, the follow-up specs and the final returned specs at the filter exit. |

| 2026-09-19 | RF-506 (slice twelve) | Ran the discriminating experiment that slice eleven designed: forced `shouldNormalizeOrderedLists` to `false` in `extensions/markdown.ts`, switched only `Enter`, and ran the single list-split test. With normalization off the output became `1. Alpha\n2. Beta\n2. Gamma` — byte-identical to the planner's intermediate result — versus `1. Alpha\n2. \nBeta\n3. Gamma` with it on. The extra line break is therefore inserted by `computeNormalizedOrderedListDocument` in `packages/editor-core/src/commands/list-edits.ts`, which should only renumber the second `2.` to `3.`. All experiment edits were reverted. | Enter failure count held at 20; `semantic-parity` + `code-editor` + `markdown` = 315/315. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0. | Slice twelve complete; the split case is not fixed yet, but the responsible component is now proven rather than inferred. | Suspicious path identified: `computeChangedRangeOrderedListNormalization` (`list-edits.ts:783`) reverse-derives a candidate block from `changedRanges` and maps internal changes back with `baseOffset = candidateRange.from + targetBlock.startOffset`. The semantic path inserts `"\n2. "` where the legacy path inserts a different shape, so the same offset reverse-derivation can land differently. Next step is to log `candidateRange`, `changedFrom/To`, `targetBlock` and the final `changes` there. If the normalizer cannot be trusted on semantic transactions, the alternative is to give semantic transactions the normalization annotation and move renumbering into `@fishmark/editor-model` — a larger, separately-scoped change. |

| 2026-09-18 | RF-506 (slice eleven) | Narrowed the list-split介入层 further. Read all three "insert-like" post-processors (`createWhitespaceInputSelectionTransaction`, `createDetachedListBlankLineInsertTransaction` and its `readSinglePlainTextInsertion` guard) and established they cannot fire on an Enter transaction because every one of them requires a single plain-text insertion with no line break. Then computed the planner's applied intermediate result with a throwaway probe: `splits a list item…` yields `1. Alpha\n2. Beta\n2. Gamma` (correct — only the ordered-list normalizer still has to renumber the second `2.` to `3.`), while the real run produces `1. Alpha\n2. \nBeta\n3. Gamma`. The single `2. Tail` case yields `Tail`, matching. | Enter failure count held at 20; `semantic-parity` + `code-editor` + `markdown` = 315/315. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0. | Slice eleven complete; the split case is not fixed. | Remaining candidates are `computeNormalizedOrderedListDocument` (which runs for every `docChanged` transaction) and the renderer's frame/selection relocation in `code-editor.ts`. Next step is to force `shouldNormalizeOrderedLists` to `false` and re-run the single test: if the extra break disappears the normalizer is responsible, otherwise the renderer layer is. |

| 2026-09-18 | RF-506 (slice ten) | Investigated the list-split failure (`1. AlphaBeta` + Enter before `Beta`). A throwaway probe proved the planner is correct — `planListItemEnter` returns `{ kind: "list-continue", edits: [{ from: 8, to: 8, insert: "\n2. " }] }`, which after post-processing should yield the expected `1. Alpha\n2. Beta\n3. Gamma`. The real run instead produces `1. Alpha\n2. \nBeta\n3. Gamma`, so the extra line break comes from a post-planner rewriter in `packages/editor-core/src/extensions/markdown.ts` — most likely `createWhitespaceInputSelectionTransaction` or `computeNormalizedOrderedListDocument` — not from `planEnter`. | Enter failure count held at 20 (slice nine) with no regressions; `semantic-parity` + `code-editor` + `markdown` = 315/315 after restoring the keymap. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0. | Slice ten complete; the split case is not fixed. | Next step is precisely scoped: short-circuit each post-planner rewriter in turn and re-run the single split test to identify the one inserting the break, then converge that rule into `@fishmark/editor-model`. Third hard lesson recorded: bulk PowerShell regex replacement of `markdown.ts` has now corrupted the file three times, so that file must be edited section by section. |

| 2026-09-18 | RF-506 (slice nine) | Fixed the leading-blank-line family in `planListItemEnter`'s empty-item exit branch: it inserted the container separator unconditionally, so a lone item promoted to body text gained a blank line. The separator is now only written when a block actually precedes the item, and a lone item at the document start exits straight into its content. | Switching **only `Enter`** on `src/renderer/code-editor.test.ts`: 21 failures before, **20 after**, with `upgrades a single top-level list item to body text without leaving a leading blank line` newly passing and no new failures. Cumulative Enter gain across slices eight and nine: 22 → 20. `markdown.test.ts` 41/41; `semantic-parity` + `code-editor` + `markdown` = 315/315 with the keymap restored. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0. | Slice nine complete; cutover not landed. | No user-visible behaviour changed and no old semantic module was deleted. |

| 2026-09-18 | RF-506 (slice eight) | Fixed the first Enter list-upgrade rule in `planListItemEnter` and measured a real net gain. Root cause of the previous two failed attempts: `isEmptyItem` is derived from `source.slice(line.contentStartOffset, offset)`, so it is **always true when the caret sits at the content start**, making the `!isEmptyItem` guard never fire. The working condition uses "is there content to the right of the caret" instead, then replaces the line with the parent prefix plus the line minus its own indentation, leaving ordinal renumbering to the document normalizer. | Switching **only `Enter`** on `src/renderer/code-editor.test.ts`: 22 failures before the fix, **21 after**, with `upgrades a nested list item to a parent item when Enter is pressed before its content` newly passing and no new failures. `semantic-parity` + `code-editor` + `markdown` = 315/315 with the keymap restored. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0. | Slice eight complete; cutover not landed. | The two `markdown.test.ts` pipe-table failures observed while the measurement switch was active were isolated with `git stash` and proven to come from routing Enter through the semantic planner (no table-draft materialization there), not from the list change. No user-visible behaviour changed and no old semantic module was deleted. |

| 2026-09-18 | RF-506 (slice seven) | Located the real entry point for the Enter promotion family: `planListItemEnter`'s empty-item parent branch, not `planScopedListItemEnter`. A throwaway probe showed `decideEnter` on `- parent\n  - child` (caret on `child`) returns `{ kind: "list-exit", edits: [{ from: 9, to: 18, insert: "-" }] }` — the full line range replaced by the bare marker, which discards the item content and explains the corpus output (`- parent\n-`, `1. Parent\n\nChild`). Both attempted fixes touched `planScopedListItemEnter`, which the probe proved is never executed for this case, so neither could have taken effect; the attempt was fully reverted. Also identified a latent offset bug: `lineTextOf` returns line-relative text while the code used match indices as absolute source offsets. | `semantic-parity` + `code-editor` + `markdown` = 315/315. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0. | Slice seven complete; cutover not landed. | Next attempt must edit `planListItemEnter` and must confirm the branch is reached (log or a direct `decideEnter` assertion) before trusting a result — both failures this slice came from editing a branch that never ran. No user-visible behaviour changed and no old semantic module was deleted. |

| 2026-09-18 | RF-506 (slice six) | Decomposed the 57 real-corpus failures by command with two controlled experiments on `src/renderer/code-editor.test.ts`: switching **only `Enter`** yields 22 failures, switching **everything except `Enter`** yields 36, and switching everything yields 57. Set difference attributes **21 failures uniquely to `Enter`** (14 of them the single list upgrade/exit rule family) and **36 to Backspace/Tab/Shift-Tab/vertical navigation**. The switch was a temporary measurement and the legacy keymap plus the opt-in `onSemanticCommands` wiring are restored. | `semantic-parity` + `code-editor` + `markdown` = 315/315. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0. | Slice six complete; cutover not landed. | This replaces the earlier category estimate with a measured priority order: Enter (21) → Backspace quote/blank (~18) → vertical navigation (~10) → Tab/Shift-Tab (~5) → table/fence stragglers. Enter is the best first target because it is one command and one module, and 14 of its 21 failures share one rule family. |

| 2026-09-18 | RF-506 (slice five) | Documented `listItemPrefix` offset semantics with a throwaway probe (`startOffset` is the line start; prefix text = `ancestorText + indentationText + markerText + markerSpacingText`), then attempted the nested-item promotion rule and added two real corpus cases to the parity harness. The harness's legacy path produced `1. Parent\n \n  2.  1. Child\n2. After` where the real corpus expects `1. Parent\n2. Child\n3. After`, proving the harness does not reproduce production behaviour for nested quote/list upgrades. The behaviour change was not adopted (no parity gain, two new mismatches, unverifiable against the real corpus) and was removed section by section. | `semantic-parity` + `code-editor` + `markdown` = 315/315. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0. | Slice five complete; cutover not landed. | Binding lessons recorded: parity is a coarse regression guard only and `code-editor.test.ts` is the judge; the next attempt must switch a single command on the real corpus to keep the change surface decidable; `git checkout --` must never roll back `editor-model` files (it silently reverted two delivered shared-layer fixes, which were re-applied); and PowerShell bulk regex replacement of `markdown.ts` has now corrupted the file twice — edit it section by section. No user-visible behaviour changed and no old semantic module was deleted. |

| 2026-09-18 | RF-506 (slice four) | Corrected the false "pre-existing `markdown.test.ts` failures" record (that file is 41/41 in both the pristine tree and the current workspace; the real pre-existing environmental failure is `generate-icons.test.ts` ICO conversion), and located the root cause of the largest failure class: `planScopedListItemEnter` has no branch for "nested item at content start promotes to its parent", emitting `\nChild` where `2. Child` is required. | Corpora `code-editor.test.ts` + `markdown.test.ts` = 314/314 after re-applying the slice-two fixes that a `git checkout --` revert had silently dropped. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; build exit 0. | Slice four complete. | The false baseline claim came from a half-edited working state, not from the product; baseline claims must be re-verified on a pristine tree. |

| 2026-09-18 | RF-506 (slice three) | Rebuilt the parity harness on real behaviour-corpus inputs (44 cases: list upgrade/split/join, quoted lists, ordered renumbering, indent/outdent, table and quote navigation, blank-run collapse) instead of invented minimal inputs, and captured the full 57-case corpus failure list with a per-category breakdown. Attempted the vertical-navigation rules (blank runs are rendered rows; only quote-marker-only and whitespace-only lines are hidden separators; collapsed blank-run anchor) — the rules matched 6 of 7 measured anchors but regressed `packages/editor-core/src/extensions/markdown.test.ts`, so the whole file change was reverted. | Corpus parity recorded as `Enter 14/18, Backspace 6/7, Tab 2/3, Shift-Tab 2/2, ArrowUp 0/4, ArrowDown 1/3` (32/44, 12 mismatches); `code-editor.test.ts` + parity 2 files / 274 tests green. | Post-revert: `code-editor.test.ts` 273/273; lint 0 errors / 8 pre-existing warnings; typecheck exit 0; full Vitest exit 0; build exit 0. | Slice three complete; cutover not landed. | The decisive structural finding: the legacy engine and the pure planners **share** `packages/editor-model`, so planner edits are production edits — a stash experiment showed the slice-two fixes are what moved the real corpus from 59 to 57, not a separate new path. Any future planner change must therefore be checked against both `code-editor.test.ts` and `markdown.test.ts`. **Correction (slice four):** the "five pre-existing `markdown.test.ts` failures" recorded here do not exist — that file is 41/41 on both the pristine tree and the current workspace. The 57-case failure list is saved outside the repository at `D:\FishMark\rf506-failures.txt`. No user-visible behaviour changed and no old semantic module was deleted. |
| 2026-09-18 | RF-506 (slice two) | Ported four real-corpus rules into the pure planners: `code-fence.ts` `planFenceDraftEnter` now pads the quote prefix on the content line and the closing marker, `enter.ts` stops padding an empty continued task item and stops carrying the empty checkbox padding when the outermost item leaves, and `backspace.ts` deletes the preceding line break at a line start so Backspace actually joins two lines instead of dispatching an empty edit. Added focused coverage through the parity measurement. | Parity 18/23 → 21/23 (Enter 12/12, Backspace 5/5, Tab 2/2, Shift-Tab 1/1, ArrowUp 0/2, ArrowDown 1/1); `packages/editor-model` + `editor-core` + parity 46 files / 504 tests. | With the keymap switched, `src/renderer/code-editor.test.ts` is 216/273 (57 failures, down from 59). Post-revert: 273/273; lint 0 errors / 8 pre-existing warnings; typecheck exit 0; full Vitest exit 0; build exit 0; architecture guard 234. | Slice two complete; cutover not landed. | The decisive methodological finding is that the minimal parity fixtures are necessary but not sufficient: four rules can be fixed while the real corpus barely moves, because its failures concentrate in nested quote/list/table combinations the fixtures do not model. The next round must drive the work directly from the existing `code-editor.test.ts` assertions and treat parity only as a regression guard. The two residual parity differences are both upward navigation across hidden rows (adjacent bare `>` separator, consecutive blank-line collapsed-row anchor) and must be pinned by real corpus assertions before the implementation changes again. No user-visible behaviour changed and no old semantic module was deleted. |
| 2026-09-18 | RF-506 (slice one) | Built the single new decision path for semantic keypresses (`packages/editor-core/src/commands/semantic-keypress.ts`) over the RF-601 adapter planners with a narrow `EditorChangeFramePort` and no second queue, plus opt-in host wiring (`onSemanticCommands`, `onSemanticChangeFrame`, revision readers) in `extensions/markdown.ts`. Added `semantic-parity.test.ts`, which runs the legacy command path and the pure planners on the same source and caret and reports a per-command gap. | Parity measured at 18/23 (Enter 10/12, Backspace 4/5, Tab 2/2, Shift-Tab 1/1, ArrowUp 0/2, ArrowDown 1/1); switching the keymap produced 214/273 in `src/renderer/code-editor.test.ts`. | Post-revert: `code-editor.test.ts` 273/273; architecture guard 234; lint 0 errors / 8 pre-existing warnings; typecheck exit 0; full Vitest exit 0; build exit 0. | Slice one complete; cutover not landed. | The five located differences are: empty task item leaves a padding space, compact fence completion loses quote padding and the closing line, Backspace at a paragraph line start does not join, an adjacent hidden `>` separator is not crossed, and consecutive blank lines land on the previous block's end instead of the visible blank row start. Existing assertions in `code-editor.test.ts` prove navigation is modelled on visible rendered rows (the extra blank row start is `hiddenBlankLineStart + 1`), which the pure planner currently violates. No user-visible behaviour changed and no old semantic module was deleted, per the roadmap rule that deleting a working route requires real runtime evidence first. |
| 2026-09-18 | RF-601 | Added `@fishmark/codemirror-adapter` as the CodeMirror side of the semantic engine: `transaction-adapter.ts` (per-view structure cache with monotonic revisions, prepare/record split, stale and foreign-session rejection, composition freeze, `EditorChangeFramePort` onto the existing queue/client), `selection-mapper.ts` (snapshot reuse plus stale/foreign-session classification), `composition-controller.ts` (pure IME state with exactly one end-of-composition recompute), `candidate-performance.ts` (honest counters including fallback reason and parsed cost), and history grouping that keeps plain input in CodeMirror's typing group while structural completion is isolated. Deleted the superseded `packages/editor-core/src/commands/editor-model-bridge.{ts,test.ts}`, activated the `codemirror-adapter` guard boundary and retired its three bridge exceptions, and added the package alias to both tsconfigs and both bundler configs. | Focused adapter + integration 6 files / 56 tests; architecture guard 234 tests. | lint 0 errors / 8 pre-existing warnings; typecheck exit 0; full Vitest 200 files / 2,585 passed + 1 skipped; build exit 0. | Acceptance `PASS`; `reports/task-summaries/RF-601.md`. The acceptance pass removed three redundant states that implied version ownership the adapter does not have (session-snapshot revision and acknowledgedRevision, plus the unused `isGeometryFrozen`/redundant assertion) and re-ran typecheck, lint, and the full suite green. Functional-runtime and platform-IME acceptance remains with RF-506/RF-902. | RF-601 is `COMPLETE`; M6 is 1/4 `IN_PROGRESS`. Candidate evidence: plain-paragraph typing is zero-full-parse on 5k/20k mixed, long-paragraph, large-table, and deep-container fixtures; document-start and reference-definition edits report explicit fallback reasons and whole-document cost. The production editor still routes through the old semantic engine, so the formal Electron corpus is not this task's evidence and was not rerun. RF-506 is now dependency-ready. |

## 8. Blockers and deviations

The user authorized the 2026-09-17 repair and reordered roadmap around maintainability/extensibility, editing/interaction, and performance. Earlier 22/38 completion records remain historical. The new review blocks semantic cutover on RF-HARDEN-001 and RF-601; RF-506 is not the next dependency-ready task. Shared render planning moves before viewport decoration migration. Runtime recovery and new-path performance evidence move to the changes that introduce those risks; final M9 validation still remains required.

Any deviation must record:

1. affected task and requirement;
2. concrete evidence that the roadmap path is invalid;
3. replacement design and dependency impact;
4. deletion impact and whether compatibility code would be introduced;
5. user approval date;
6. corresponding update to both `roadmap.md` and this file.

Performance difficulty, test volume, or implementation inconvenience alone is not a valid reason to weaken document truth, editing behavior, round-trip safety, or deletion requirements.

## 9. Progress update procedure

When starting a task:

1. Confirm all dependencies are `COMPLETE`.
2. Change only that task to `IN_PROGRESS`.
3. Set `Current task` and `Next required skill` at the top.
4. Re-read the roadmap task, current accepted decisions, and deletion obligation.

When implementation finishes:

1. Add focused command output/report paths to the ledger.
2. Add full gate evidence required by the roadmap.
3. Change status to `DEV_DONE`.
4. Append one evidence-log row.
5. Invoke architecture acceptance and task acceptance.

When acceptance passes:

1. Record both acceptance results and task-summary paths.
2. Verify the task's required deletions and documentation updates.
3. Change status to `COMPLETE`.
4. Update milestone counts/status.
5. Select the next dependency-ready task; do not start it in the acceptance diff.

When acceptance fails:

1. Keep the same task active.
2. Record findings in its evidence-log row.
3. Return to `$fishmark-task-execution` for the same task.
4. Do not advance milestone counts.

## 10. Final completion checklist

- [ ] All 38 task rows are `COMPLETE`.
- [ ] All 11 milestone rows are `COMPLETE`.
- [ ] Every milestone deletion item is checked.
- [ ] Main owns canonical text/revision/save/disk/conflict/recovery state.
- [ ] Revisioned edit transport is the only editing path.
- [ ] Recursive parser/cache differential tests pass.
- [ ] Nested list/blockquote semantics match top-level leaf capabilities.
- [ ] Enter/Backspace/Tab/arrows/selection/IME/undo/round-trip matrices pass.
- [ ] `packages/editor-core` and every compatibility path are gone.
- [ ] Renderer presentation contains no business workflow ownership.
- [ ] Performance budgets pass with fresh evidence.
- [ ] Playwright data-safety/editing scenarios pass.
- [ ] Electron sandbox/CSP/sender/path security gates pass.
- [ ] `npm run lint`, `typecheck`, `test`, and `build` pass.
- [ ] Bundle, performance, editing-experience, and E2E gates pass.
- [ ] Architecture acceptance result is `PASS`.
- [ ] Task acceptance result is `PASS`.
- [ ] Stable docs, backlog, progress, test cases/report, package READMEs, and task summaries agree.
